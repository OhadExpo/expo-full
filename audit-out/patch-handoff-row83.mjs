// Handoff row 83: his nine club-zone asks of 13.9 ~17:00, mid-run.
import fs from 'node:fs';
const f = 'docs/HANDOFF-2026-09-06.md';
const L = fs.readFileSync(f, 'utf8').split('\n');
const i = L.findIndex((l) => l.startsWith('| 82 |'));
if (i < 0) throw new Error('row 82');
if (L.some((l) => l.startsWith('| 83 |'))) throw new Error('row 83 exists');
L.splice(i + 1, 0, '| 83 | 2026-09-13 ~17:00, nine messages in a row: "the google calendar is not synced with bhbc.. make sure all the practices and scrimmages are logged in" · "yesterday amit gershon worked out" · "game vs who and where at?" · "where are all the shootarounds? i need bhbc better updated and always synced" · "this can be better displayed" (weight room) · "if there was a LATER report for pain… update it here since the pain is 9" · "dusty worked out for 30 minutes on september 7th" · "all the hebrew titles everywhere are not aligned right" · "find it and fill it it looks bad. the design is awful and it\'s not synced" (game minutes) | **data live on production (store), code built, NOT deployed** (queue H1–H9). (1) `scripts/sync-bhbc-calendar.mjs` reads the "Bnei Herzliya - 2026/27" Google Calendar (BB = practice, Weight Room = lift, Shootaround, Scrimmage vs X @place, …game/BCL/G1 = game; buses, flights, dinners, birthdays skipped) and merges it into `expo-bhbc-fixtures`: 33 → 53 (Aug 20 – Oct 12: 33 practices, 5 lifts, 4 shootarounds, 2 scrimmages, 9 games); games keep their league facts and take the calendar\'s clock; the 3.9 "game" became the calendar\'s scrimmage vs Maccabi at Hadar Yosef 17:00; 7 seeded practices the calendar does not have were dropped; snapshot `backup-expo-bhbc-fixtures-*.json`. **Standing sync is blocked on access:** the calendar is the club\'s (Ohad is a reader), it is not public (no public iCal), the settings page shows no secret address to a reader, the web client\'s own API refuses cross-origin calls, and the service account needs the calendar shared with it — the script takes an ICS URL or the JSON a session pulls through the Calendar connector. (2) The zone knows `scrimmage` and `shootaround` (colours, labels, Hebrew משחק אימון / שוטאראונד); every schedule chip — month, week, list — says "vs Opponent · venue" for games and scrimmages (`fxWhere`). (3) Lifts logged with a snapshot: Dusty Hannahs 7.9 30 min; Amit Gershon 12.9 60 min (his usual; the note says minutes were not given). (4) Medical board and overview show the LATEST reported pain — the newest rehab-progress entry with a score (9), not the injury\'s own field (1). (5) Weight room: minutes inside each lift tile, DUE as chips, day headers dated on hover. (6) Shared `Card`: a zero-padding card (the zone\'s tables) insets its title 14px and stops the header strip bleeding past it — this is what put Hebrew titles flush on the edge, both apps. (7) Game-minutes modal: jersey · name · minutes beside it · DNP, a summary that cannot scramble in Hebrew; minutes NOT filled — the league has published no box score for the Winner Cup games, so they still come from the coach. Photographed as the physio in Hebrew: `audit-out/bhbc-he-*.png`, `bhbc-he-minutes-modal.png`. |');
fs.writeFileSync(f, L.join('\n'));
console.log('row 83 written');
