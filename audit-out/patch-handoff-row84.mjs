// Handoff row 84: the club calendar solved without permission, and the box score.
import fs from 'node:fs';
const f = 'docs/HANDOFF-2026-09-06.md';
const L = fs.readFileSync(f, 'utf8').split('\n');
const i = L.findIndex((l) => l.startsWith('| 83 |'));
if (i < 0) throw new Error('row 83');
if (L.some((l) => l.startsWith('| 84 |'))) throw new Error('row 84 exists');
L.splice(i + 1, 0, '| 84 | 2026-09-14/15: "for 3 figure it out that\'s not a good answer. you need to find a way on your own" (the club calendar) · the 3.9 box score photographed · "i will add the stats for eilat" | **built, NOT deployed.** **The calendar syncs itself now, with nobody\'s permission.** He only READS "Bnei Herzliya - 2026/27", so: `/calendar/exporticalzip` gives a subscriber only the calendars he OWNS (measured - it returned his own + birthdays); the settings page shows a subscriber no secret iCal address; `clients6.google.com` refuses a hand-built SAPISIDHASH ("Origin doesn\'t match Host"); the service account cannot be added to an ACL he does not control. What works: the Calendar web app asks its OWN origin for a date range (`POST /calendar/u/0/sync.prefetcheventrange`), so `scripts/fetch-bhbc-calendar.mjs` opens a BACKGROUND tab in his signed-in Chrome, lets the app make that call, captures the exact body Google built, and re-issues it with the range we want; per event `[5]` is the title and `[35][1][0]` / `[36][1][0]` are start and end in epoch ms - located by searching for a known event\'s instant, not guessed. 107 events Aug 2026 → Feb 2027. **Proven against the Calendar connector event by event:** 47/47 identical end times, and the replay was FRESHER - it already had the six practices Amit created at 09:11-09:15 on 14.9 and the two he moved, which yesterday\'s pull did not. The zone went 53 → 80 fixtures (48 practices, 6 shootarounds, 3 scrimmages, 18 games, 5 lifts). Wired into the twice-daily sync as soft steps, and the sync now survives the failure that killed the 10:45 run on 14.9: a normal Chrome holding the profile without a debug port, where the launch only hands its argv to the running instance - it starts the signed-in CLONE (`chrome-debug-harvest`) instead. **The box score** he photographed (Maccabi TA 84-86 Bnei Herzliya, 3.9) is in the zone: nine players\' minutes, the Min column checksummed to exactly 200:00 before anything was written; `#23` is printed "D.BREWTON" on the sheet and every other jersey matches the EXPO roster, so it went in as DJ Broughton - the one line to check. Minutes are a fact and RPE is a judgement: `applyGameMinutes` now records minutes with NO rpe the way the gym rule has since 08-21 (zero load, attended, out of ACWR) and computes the load the moment he sets one; gated in `verify-game-load.mjs` and proven by breaking the fix (4 named failures, exit 1). Re-shooting the games tab then caught two real defects: the 3.9 fixture had become a `scrimmage` (what the calendar calls it) and the minutes list only looked at `game`, so the box score had nowhere to show - scrimmages count now and the row says which kind it was; and a fixture with no opponent printed "Opponent" as if that were a club (now TBD). Eilat\'s minutes are his - he said so. 390px measured on /coach/billing and the zone: the page does not scroll sideways on either; what sits past the edge is inside the deliberately scrolling tab strip and month table. |');
fs.writeFileSync(f, L.join('\n'));
console.log('row 84 written');
