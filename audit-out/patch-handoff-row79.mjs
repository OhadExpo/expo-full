// Handoff row 79: the 09-12 evening after row 78 (21:00 → the host refresh).
import fs from 'node:fs';
const f = 'docs/HANDOFF-2026-09-06.md';
let s = fs.readFileSync(f, 'utf8');
const lines = s.split('\n');
const i = lines.findIndex((l) => l.startsWith('| 78 |'));
if (i < 0) throw new Error('row 78');
if (lines.some((l) => l.startsWith('| 79 |'))) throw new Error('row 79 exists');
const row79 = '| 79 | "keep working" · "not close to done" · "figure it out on your own so much unsolved work" (2026-09-12, 20:40 → 22:30) | **built, NOT deployed** — (1) `scripts/verify-english-literals.mjs` is a build gate (uppercase `>LABEL<` and placeholders in any view that imports the translator; data-shape allowlist; `--report`): 113 findings on the tree → 0 by `scripts/codemod-wrap-english-literals.mjs` + 70 keys; break-tested; the pattern later widened to labels ending in an ellipsis, which caught four `LOADING…` and the analysis strip title. (2) The last English on the Hebrew billing / tasks / waitlist screens (sheet card column, tasks rail, list/board, two sentences); month names turn in Hebrew wherever they were hand-built — `monthAbbr()` / `dayMonthShort()` in `dates.js` ("7 באוג׳", the ב is not optional) — and the relative words (TMRW, weekdays) translate at render; dumped: 0 English UI lines on the three routes. (3) The 96 keys added since the deploy judged: 5 flagged, 4 agreed (KPI tiles say "· גיליון", PRICING CTA = תמחור, FOCUS FOR = לשבוע), 1 kept. (4) **E10 — the club zone as the physio reads it in Hebrew**, measured tab by tab with `audit-out/probe-bhbc-coverage.mjs` (now measures the tab the page opens on and prints words per tab) and `probe-bhbc-find-text.mjs` (finds the element behind any English word — that is how "Show 12 more", the GAME chips and the weekday row were located): injury-board headers, Month/Week/List, weekday row, month heading, GAME / GAME DAY, the matchday label, add-session types, Player / Margin, pre-season and live badges, three empty states, the regression ladder, competition names, the league\'s position names — 33 keys, judged, 3 fixed; what stays English is names, teams, nationality codes and stat/MD abbreviations. (5) E9 `audit-out/dict-hygiene.mjs`: 10 casing pairs across both dictionaries, all deliberate singular/plural or noun/imperative splits; nothing to unify. (6) `deploy-0911` carried every coach commit; its build FAILED the new gate on a bare `SYNC →` — `WorkoutReview.jsx`\'s two 09-11 evening Hebrew commits had never been carried over; taken from `bhbc-hebrew` (coach-only file; the athlete tree sits outside the language provider). Candidate green and pushed. |';
lines.splice(i + 1, 0, row79);
fs.writeFileSync(f, lines.join('\n'));
console.log('row 79 written after line', i + 1);
