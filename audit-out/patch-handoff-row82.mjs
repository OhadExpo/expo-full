// Handoff row 82: the 09-13 morning billing mandate. Written mid-run; the
// closing numbers (revisions on disk, payments, reconciliation) are stamped by
// the line marked FINAL at the end of the shift.
import fs from 'node:fs';
const f = 'docs/HANDOFF-2026-09-06.md';
const L = fs.readFileSync(f, 'utf8').split('\n');
const i = L.findIndex((l) => l.startsWith('| 81 |'));
if (i < 0) throw new Error('row 81');
if (L.some((l) => l.startsWith('| 82 |'))) throw new Error('row 82 exists');
const row = '| 82 | 2026-09-13 08:20 → : "the billing is not even close to 10% ready. re-run every history filed on רשימת מתאמנים and everything you can possibly think of to make it 100%. 5 more hours of autonomous work. i trust you" | **built, NOT deployed** (queue F1–F9). **What was wrong:** the 09-04 harvest had sampled every 4th revision (285 files of ~2,600) and the parser kept three date columns — so most payment-date changes and every counter reset between samples were invisible. **Done:** (1) the signed-in profile cloned into two headless Chromes (:9225, :9226) that fetch EVERY revision, detached and resumable (Google paces exports at ~10s/file per account, so ~6 files/min whatever the parallelism — ~6h for the whole history; `harvest-full1b.log` / `harvest-full2.log`; the fast path is `scripts/drive-sa.mjs harvest` through the gsheets service account, which needs the two sheets SHARED with `mcp-gsheets@expo-music-495221.iam.gserviceaccount.com` as viewer — that share is a permission grant the session was not allowed to perform; 20 seconds for him); (2) every revision id timestamped from the version-history panel\'s own endpoint (`audit-out/sheets/revisions.json`, 2,550 ids; the newest edits — r2606–r2626, the sheet as of 12.09 — found by probing above the highest file); (3) `scripts/parse-roster-timeline.py` keeps EVERY column under all 12 header variants the sheet had since 2021 (entries, card type, phone, email, position, notes included) → `cells.jsonl` + per-client `timeline.json`; (4) `scripts/derive-payments.mjs`: a payment = a change of the payment-date (or card-start) cell, with the counter as it stood before the reset, the rate in force, an ESTIMATED amount by method (monthly / card / per_session / count; unknown when the sheet gives no price) and a confidence; attendance = every counter increment, dated by its revision window; rate changes; month-by-month reconciliation against ניהול פיננסי (on 20% of revisions June 2026 lands within ₪570 of the sheet\'s coaching total without the club; the gaps elsewhere are the unharvested revisions); (5) `revenue_sheet_event` grew 14 estimate columns (kinds payment · card_start · session · rate_change) and `revenue_cell_history` holds the raw cells — owner-only, `verify-revenue-private.mjs` 0 leaks from five seats; snapshots taken before every write (`backup-*.json`); (6) the finance sheet turned out to be five months old (created 2026-03) — its April/May 2026 tabs, since removed, recovered from its revision history and imported (`import-finance-history.mjs`); (7) `/coach/billing`: month table gained ROSTER ESTIMATE and GAP columns; every client row expands to its full history (paid on · rate · cycle · estimate · method dot · notes/unpaid); the athlete page\'s billing section shows the same for that client (`SheetBillingHistory`); (8) the twice-daily sync now harvests new revisions in a background tab of his Chrome, re-parses, re-derives and imports (`harvest-new-revisions.mjs`, cells only above the last imported revision); (9) 32 new Hebrew lines judged, 3 fixed; (10) `attendance-vs-sheet.mjs`: roster counts beside EXPO-logged workouts — gym clients log nothing in the portal, as expected. **Kept as written in CLAUDE.md:** no amount is presented as fact; every estimate carries its method and confidence and is reconciled to the monthly totals on screen. **Open:** the harvest finishes after the shift (resumable; `bash audit-out/refresh-billing.sh` re-parses everything on disk); 39 of 55 sheet clients are historical people with no EXPO record (shown as "no record", never created); Bit / Green Invoice / bank exports still wait on him. |';
L.splice(i + 1, 0, row);
fs.writeFileSync(f, L.join('\n'));
console.log('row 82 written');
