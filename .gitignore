node_modules
dist
.vercel
_nord_tmp
_*.py
*.pyc
__pycache__
.DS_Store
install-nord-fonts.ps1
backups/

.vercel
.claude/
.icon-previews/
.playwright-mcp/
.tmp.driveupload/
sheets/
expo-il/.qa/
scripts/meakav-out/
_archive/

# Local-only investigation artefacts. Chrome leveldb dumps contain real
# session tokens and third-party API keys (Discord, AWS, Google OAuth,
# Azure). NEVER commit anything from these paths.
claude pctolap/
New Programs/
*.png
*.jpg
*.jpeg
# ...but public/ holds real deployed assets (icons, og-image), not screenshots.
!public/og-image.png
amit-*.png
chat-*.png
coach-*.png
trainee-*.png
verify-*.png
demo-*.png
he-demo-*.png

# Training program source files dropped at repo root (xlsx + handwritten
# PDFs). They feed the import scripts under scripts/import-*.cjs and
# stay local-only — the imported state lives in Supabase. Anchored to
# the root with a leading slash so _branding/*.pdf and other tracked
# subdir binaries are unaffected.
/*.xlsx
/*.pdf

# One-shot investigation outputs from data-quality work (SuperSet rot,
# data audits, etc.). Re-runnable from the source script — committing
# them just rots fast.
scripts/audit-*.json
scripts/verify-*.json
scripts/fix-*.dryrun.json
tmp-drive-xlsx/

# Throwaway CDP / diagnostic scripts — one-shot probes against the
# running dev app or Supabase. Re-creatable from memory; committing
# them puts a secret one careless line away.
scripts/cdp-*.cjs
scripts/check-*.cjs
scripts/diag-*.cjs
scripts/seed-*.cjs
scripts/test-*.cjs

# Per-user content folders that aren't repo state.
/Exercise Database/

.env*.local

# Local business docs — must never be committed
_biz_extract/

# Dev server log captured during local audit sessions
.dev-server.log

# EXPO Performance Center business/legal/financial folder (confidential —
# strategy, finances, partnership/legal, lawyer + accountant recordings).
# Lives in the repo dir; must NEVER reach the public GitHub repo.
EXPO - המרכז למצויינות-פיתוח אתלטית/

# Loose dev screenshots / audit artifacts at repo root — never repo state.
/*.png
/*.jpg
/*.jpeg
.parity-audit/
