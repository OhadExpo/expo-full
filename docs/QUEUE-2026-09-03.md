# Live queue — 2026-09-03 (5-hour block, 00:52 → 05:52)

Captured from his messages as they arrived. Nothing here is closed until it is
verified against observed output.

## Closed

| # | Ask | Evidence |
|---|---|---|
| 1 | "i can't screenshot anymore and paste it here, fix it immediatly" | The AutoHotkey watcher `claude-image-paste.ahk` had died — no process running, last clip written 13:40 the previous day. Restarted (pid 8632) and proved end-to-end: the image on his clipboard wrote `clip_20260903_113725_967.png`. Nothing was wrong with his clipboard. |

## Open — the COLUMN cluster (he has asked three separate times, "i keep asking this request")

| # | Ask |
|---|---|
| 6 | "all the text boxes everywhere on bhbc should start horizontally from the same spot. so it match like columns" |
| 7 | THIS WEEK row (10:30 practice / 12:00 practice) "doesnt align horizontally with the rows above like a column. this should be a rule everywhere" |
| 12 | Athlete profile: "the stats on the right should be ordered like columns. i keep asking this request. figure it out everywhere and fix and align it" |

Treat as ONE job: a shared column grid across the head-coach-report rows and the
athlete profile, plus a gate that fails when any two rows in a card disagree on
their column x-positions.

## Open — the rest

| # | Ask | Notes |
|---|---|---|
| 2 | "is the dashboard always updated? pulling info from the רשימת מתאמנים?" | A question. Answer from evidence: the dashboard reads Supabase `store`, NOT the Google Sheet. The twice-daily sheet sync is still blocked on him sharing the sheet. |
| 3 | expo > roster > athlete card: portal + edit buttons "too big and text is misaligned", "also the buttons are too wide" | MEASURED: PORTAL renders `justify-content: normal` so its label sits 53.3px left of centre at 1500 (26.9 at 390); EDIT has `center` and measures 0. Both 207.2px wide for ~66px of content. Two call sites in `TraineesView.jsx`. |
| 4 | "the metrics and the video have a slight delay/no sync when i review videos" | Not yet investigated. |
| 5 | NEXT GAME: "doesnt have the same vertical empty space above the text as beneath it. i asked for a fix 5 times already" | Element-anchored measurement has failed repeatedly (it reported an impossible -1px). Switch to PIXELS on a cropped screenshot as ground truth. |
| 8 | League "last game" is wrong — "we had a playoff game against holon" | ROOT-CAUSED, see below. Not a UI bug. |
| 9 | "full history should be longer or easier to view. takes too little space" | |
| 10 | "put the bw graph at the bottom" | |
| 11 | "it doesnt re-order the column based on up and down when i click on the column headers" | |

## #8 — league last game: the data, not the display

Read from `store['expo-bhbc-league']` (authenticated; RLS returns nothing anonymous):

- `updatedAt` **2026-08-15**, season 2025/26, cYear 2026.
- 194 games, dates **2025-10-12 → 2026-06-22**, rounds 1–32 — the REGULAR SEASON only.
- DJ Burns has 13 log entries; the newest by date is **2026-05-25 vs מכבי תל אביב**,
  which is exactly what the card displays.
- "חולון" appears in 26 games in the feed but **never as a BHBC opponent in his log**,
  and there are **no playoff rounds in the payload at all**.

So the card is faithfully reporting the data it has, and the data is missing the
playoffs. `scripts/_bhbc-sync-league.mjs` scrapes board=5 (Ligat HaAl regular
season) and has not been re-run since 15 August. The fix is in the SYNC, not the
component — and until the playoff rounds are actually scraped, no playoff game
may be typed in by hand. Blank beats invented.
