# HANDOFF — 2026-09-06 · the BHBC replan night

Written to be read cold by a conversation that has none of this in context.
Every claim in it was checked against disk, git, the database or a live render —
the audit log is at the end, ten passes, with what each one found.

---

## 0 · How to restore context in four minutes

```bash
cat ~/.claude/projects/C--Users-Administrator-Desktop-expo-full/memory/MEMORY.md   # index
cat .../memory/project_bhbc_replan_2026_09_06.md                                   # tonight
cat .../memory/project_handoff_2026_09_06.md                                       # last night
git log --oneline -40
cat docs/HANDOFF-2026-09-06.md                                                     # this file
```

Ohad's own restore triggers, already in memory: the single word **"bbq"**, and
the phrase **"check my memory bro"** — both mean *read MEMORY.md + the newest
handoff + `git log` from disk, then reply with state and the open list.*

---

## 1 · Who and what

- **Ohad** — solo S&C coach and עוסק מורשה, ~20 private clients, plus Bnei
  Herzliya BC (BHBC) as a club. Product owner, designer and QA of EXPO. Blunt,
  Israeli-direct; wants deliverables, not options. Push back when the reasoning
  is wrong.
- **EXPO** — Vite + React + Supabase + Vercel. Athlete portal at `/athlete`,
  coach app at `/coach`, club zone at `/coach/bhbc`. `master` auto-deploys to
  `expo-app.co.il` on push.
- **The club zone** is used by BHBC staff: a head coach, assistant coaches, and
  a physio (`tomerlich11@gmail.com`) who reads **Hebrew**.
- Universal password on every seat, prod included: `1234`.

## 2 · The rules that govern this work

These are not suggestions; breaking one is the failure mode he penalises.

1. **Never build or test on the live site.** Work on a branch; verify locally or
   on a preview. **ASK before any production deploy.**
2. **The dev server shares the PRODUCTION Supabase.** Anything that writes must
   snapshot first and restore after. Trainee-visible tables are off limits for
   trials.
3. **Never invent training or financial data.** Blank beats wrong. An athlete
   with no lift reads "never", not "0 days".
4. **Never use "cure", "diagnose" or "fix"** in UI strings, code comments or
   client-facing text. Use "manage", "load management", "rehab strategy".
5. **Verify before declaring done** — from the real seat, on the real path, with
   observed evidence. A green build is not a working feature.
6. **Never `git add -A` / `git add .`** — stage named paths.
7. Hebrew for athletes and staff is **written, not translated**: masculine
   singular, short, spoken Israeli. Exercise names stay English.
8. Israeli VAT is 18%; VAT-inclusive → pre-VAT multiplier **0.8475**.
9. Marketing (`expo-il/`) and the demo surfaces stay in parity with the product.

## 3 · State at the moment of writing

| | |
| --- | --- |
| Branch | `bhbc-hebrew` |
| HEAD | `1cdb4633d24ea76e44661fab92c0f6094900ffe5` |
| Ahead of `master` | **1,231 commits** (grows as work continues) |
| Deployed | **Nothing from this branch.** Production is on the last `master` commit and has been all week |
| Diff vs master | **520 files, +89,133 / −4,270** (whole tree; the 428 I first wrote was scoped to src+scripts) |
| Biggest files | `src/BhbcView.jsx` +4,979 · `src/PlansView.jsx` +2,318/−442 · `src/CoachDemo.jsx` +1,463/−645 |

**Nothing ships without Ohad saying yes.** That decision has been open since
2026-09-02 and is still open.

## 4 · What happened tonight, in his words and in order

1. *"log in the prveious practices and today francis worked out"* → the practice
   backlog and two lifts were logged (earlier block).
2. *"today dj and nate worked out for 30 minutes each"* → logged; the roster has
   **two DJs**, so the tool refused to guess and asked. He answered **DJ Burns
   (#30)**.
3. *"i want a display on bhbc of who worked out when (and what players need to
   workout soon)"* → built as a card.
4. *"the weight room design is still not good enough and it should be a full
   tab"* → rebuilt as a tab.
5. **The pivot.** He sent his Google Sheet and said: *"that's litterally the main
   use of this table. wtf have we been doing up until now? plan a way better
   system im so not satisfied with what we currently have"*, then *"replan the
   entire bhbc system"*, *"and apply with way better thought and outcome"*, then
   *"also replan the entire dashboard of bhbc (main page - some stuff
   unnecessary, some stuff can be combined, some are lacking. use everything you
   can. every skill you have for this"*.
6. *"the pdf design is still too spacious and ugly, not branded enough"*.
7. *"show me on a local chrome host everything that's changed and undeployed...
   perfectly viewable and designed perfect for me to judge"* and *"show me
   before and after for every thing"*.
8. *"can you create a full deatiled handoff for a new conversation... or is it
   too complicated at this stage and stuff will get lost?"* → this file.

## 5 · His sheet, read in full

**File:** `BHBC Schedule`, id `1sv3MyrRsmHkV5tdBkhtk55mxreGCoSIxC-HnCOmBNa0`.
**Tabs:** February, January, October, September, August (2025-26 season).
**Local copies:** `audit-out/bhbc-sheet/{August,September,October,January,February}.csv` —
TRUE csv exports, by pinned gid: August 1565508632 · September 536717987 ·
October 1828409130 · January 1985844099 · February 1343858464.
**Re-download:** `node audit-out/read-bhbc-sheet-all.mjs` (uses the signed-in
debug Chrome; the Drive connector and the mcp-gsheets service account cannot see
this file).

### The spine, row by row

| Row | Holds | Example from 20/08/2025 |
| --- | --- | --- |
| Date | one column group per calendar day | `20/08/2025` |
| Day | weekday | `Wednesday` |
| Time | session shape | `Early Practice` · `Late Practice` · `Double Day` · `OFF` in August; `GAME-DAY` appears from September on |
| Practice Time (MIN) | total minutes | `84 Min` |
| Contact Time (MIN) | contact inside them | `13 Min` |
| Density % | contact ÷ practice, banded | `15.5%` → `Low Intensity` |
| Total Available Players | headcount | `9 Players` |
| Basketball Notes | plan label | `Basketball Plan` / `Plan A` / `Plan B` |
| S & C Scale | quadrant + minutes | `Q1` · `12 Min` (also `8 Min + 6 Min`, `Warm-Up`) |
| S & C content (Feb only) | free text | `- BW Strength - Dynamic Streching - Ladders` |
| Athlete × day cell | restriction code | `1`–`5`, or `OFF` |
| PT Notes / S&C Notes | note lanes per day | measured below |

### His legends, unchanged in the app

- **Availability index:** 1 No Restriction · 2 Minutes Restriction · 3 Non
  Contact · 4 Out–Medical · 5 Out–Personal. (EXPO's `AVAIL` map already matched
  these five, one-for-one.)
- **Density scale:** under 20% Low · 20–25% Moderate · 25–30% High · above 30%
  Very High.
- **Volume:** "Above 90 Min = High Volume".
- **Quadrants:** Q1 Low Volume & Low Intensity · Q2 High Volume & Low Intensity
  · Q3 Low Volume & High Intensity · Q4 *"Moderate"* Volume & High Intensity.

### The note lanes, counted rather than characterised

August, September and October carry TWO note lanes per day (PT Notes and S&C
Notes). January and February carry ONE (S&C only) — the PT lane was dropped
somewhere between October and January.

| Month | note columns | cells with anything in them |
| --- | --- | --- |
| August | 24 | 1 |
| September | 60 | 11 |
| October | 50 | 9 |
| January | 22 | 17 |
| February | 22 | 7 |

So they ARE used, but thinly — 45 filled cells across five months of days ×
athletes — and the widest version (September, 60 columns) is the least used
per column. Three lanes a day made the grid unreadable and the PT lane did not
survive the season. That is his own design lesson, not mine: the grid is for
**codes**; notes belong one click deep.

*(An earlier draft of this handoff said the lanes were "almost always empty".
The audit counted them. They are sparse, not empty — and January, the sparsest
grid, is the most annotated month.)*

## 6 · The census of what the zone had instead

Counted on the live Overview before touching it (screenshot:
`audit-out/overview-now.png`):

- **Seven cards, ~2,150px** of scroll.
- The **next game** printed three times (Head Coach Report, Today, Next Game).
- **Availability 8/2/0** three times (report, Today, a board column).
- **Medical** four times (report list, S&C Brief count, MED buttons, Medical tab).
- **Never held a value:** Team Snapshot's Avg ACWR / Flagged / 7-day load; the
  board's ACWR column (`· baseline` ×10), 7D (`—` ×10), the 14-day sparkline
  (blank ×10) and Readiness (`no check-in` ×10).

Those all need an **sRPE per athlete per session** and a **wellness check-in**.
He collects neither. That is the whole diagnosis.

## 7 · The principle now encoded

> Show what is maintained. Compute what can be computed. Nothing else earns a
> pixel.

**Maintained:** availability per athlete per day · practice and contact minutes
· what the weight room did · medical status, onset, RTP target · fixtures.
**Not maintained:** RPE per session · wellness check-in.

Therefore ACWR, readiness, monotony and strain stay in the product but appear
**only when their data exists**. `hasLoad` and `hasRead` in `LoadBoard` are that
rule in code.

## 8 · Every change, file by file

### `src/BhbcView.jsx` (the club zone, ~5,000 lines)

| What | Where | Why |
| --- | --- | --- |
| `WeightRoomTab` | new component, rendered for `view === 'weightroom'` | his month grid |
| `NAV_TABS` | added `['weightroom', tr('Weight Room')]` between Schedule and Medical | training before the record |
| Grid cells | tint = restriction code, orange square = logged lift | one cell answers both questions |
| No future columns | `if (iso > today) break;` | half the board was empty future |
| `availPerDay` | counts cells with code 1 | his "Total Available Players" row |
| `roomDays` | built from **logged sessions**, not fixtures | a scheduled lift nobody attended is not a day in the room |
| Due strip | one line of names, longest gap first | replaced a second card repeating all ten names |
| `DENSITY_BANDS`, `densityOf` | module level | his bands, and `null` unless both numbers exist |
| `quadrant` | derived: volume >90, intensity ≥25% | his Q1–Q4, computed |
| `contactMin` | on the fixture; input in the week planner (non-lift only) | the one new thing asked of him |
| `DensityBit` | shown on today's chip and on past practices | one component so thresholds cannot drift |
| Overview | S&C Brief and Next Game removed; Team Snapshot conditional; sessions panel merged into the report via `bare` | 7 cards → 3 |
| `LoadBoard` | `hasLoad` / `hasRead` gate four columns and the band legend; `lastLift` column added | nothing empty on screen |
| Hebrew | RTP ladder, pain gate, referral line, availability pill, readiness headlines, `Update ›`, `1 record`, body parts | the physio reads this |

### `src/bhbcHe.js`
~70 new keys across: availability + readiness, the RTP ladder and its two safety
rules, the medical row action, the weight-room board and tab, month names, the
load board, density bands, the quadrant meanings.

### `src/dates.js`
`fmtPrettyDate` and `fmtCompactDate` turn the **month word** when the app is in
Hebrew (`localStorage['expo-lang'] === 'he'`, read at call time in a try/catch).
Order never changes — day, month, year, both languages. `fmtNumericDate` is
untouched. Unparseable input still passes through unchanged.

### `src/ClientPortal.jsx`, `src/i18n.js`
`READINESS GRAPH →` and `NOTE` translated; the arrow turns with the text.

### `src/themes.css`, `src/PlansView.jsx` (the printed block)
- Row is a two-column grid: name/sets left, cues right, cyan spine between.
- Removed: the 266mm day floor, the 232mm first-page floor, the 17mm row floor,
  and the forced page break per day (days flow; a day still never splits).
- Masthead: EXPO black band, mark on a **white plate** (the white-ink logo file
  is invisible on black and a CSS invert is not safe to hand a printer), title
  reversed out, athlete in cyan, 3px cyan rule.

### Scripts added
| Script | Does |
| --- | --- |
| `scripts/bhbc-log-lift.mjs` | `LIFTS="name:minutes,..." DATE=… ` — logs weight-room sessions, refuses ambiguous names, snapshots, reads back |
| `scripts/build-bhbc-replan.mjs` | builds the replan page |
| `scripts/build-tonight.mjs` | builds the before/after page |
| `scripts/serve-page.mjs` | serves any page on its own port with the app's fonts |
| `scripts/shoot-prod-vs-branch.mjs` | screenshots live production and the branch, same seat/route/width |
| `audit-out/read-bhbc-sheet-all.mjs` | downloads every month tab by name |
| `audit-out/analyse-bhbc-sheet.mjs` | prints the sheet's structure and typed notes |
| `audit-out/test-density.mjs` | end-to-end density test **that restores the data** |
| `audit-out/probe-bhbc-coverage.mjs` | counts Latin words per club-zone tab in Hebrew |
| `audit-out/probe-athlete-coverage.mjs` | the same for the athlete portal |
| `audit-out/shot-weightroom.mjs` | screenshots any zone tab, any seat, any language |

## 9 · Data operations performed on production

All reversible, all verified by read-back.

| When | What | Restore point |
| --- | --- | --- |
| 18:26 | Nathan Knight, 30 min Lift, 2026-09-06 | `audit-out/bhbc-state/loads-before-lift-2026-09-06-1826.json` |
| 18:27 | DJ Burns, 30 min Lift, 2026-09-06 | `…-1827.json` |
| 19:2x | density test wrote `contactMin` to one fixture, then restored the whole key | `audit-out/bhbc-state/fixtures-before-density-*.json` |

**Database now:** roster 10 · fixtures 33 · **fixtures with `contactMin`: 0**
(restored) · 72 lift sessions logged in total. Newest: 06-09 DJ Burns 30 min,
06-09 Nathan Knight 30 min, 05-09 Daeshon Francis 30 min, 05-09 Nathan Knight
60 min. Every one carries `rpe: null, load: 0` — the gym is minutes only.

## 10 · The three local pages

```bash
node scripts/build-bhbc-replan.mjs
node scripts/serve-page.mjs audit-out/bhbc-replan.html 4180     # the plan

node scripts/shoot-prod-vs-branch.mjs                            # optional, refreshes pairs
node scripts/build-tonight.mjs
node scripts/serve-page.mjs audit-out/tonight.html 4181          # before / after

node scripts/build-recap.mjs
node scripts/serve-recap.mjs 4179                                # every undeployed commit
```

`tonight.html` leads with **live production beside this branch** — real
screenshots of `expo-app.co.il` against the local build, same seat, route and
width, minutes apart. Images are eager on purpose: lazy ones never render in a
full-page capture.

## 11 · Open, waiting on Ohad

1. **Deploy or not.** 1,229 commits. Nothing ships without a yes.
2. **Contact minutes** — will he type one number per practice? Density, the
   intensity band and the quadrant cannot exist without it.
3. **His Q4 label** — the legend says Q4 is "Moderate Volume & High Intensity"
   while Q2 is "High Volume". The app puts Q4 in the high/high cell because it
   is the only one left. Which did he mean?
4. **ACWR / readiness off the dashboard** until an RPE exists — agreed?
5. **The dashboard grid** — last 14 days or the whole month? (Currently the
   grid lives only on the Weight Room tab; the board carries LAST LIFT instead,
   deliberately, to avoid re-introducing duplication.)
6. Older, still open from previous nights: whether to cache the coach roster
   offline; dashboard offline zeros → dashes; what `›` means in Block #16's
   sets/reps (it renders as `3×>` in the PDF and is real stored data);
   `/demo/coach` showing 6 nav items against the real app's 9; whether the login
   screen should speak Hebrew.

## 12 · The queue — what is done, and exactly how to resume what is not

He cancelled the running block with: *"cancel the 3 hour work and finish the
handoff prompts. make sure all undone work and qeued tasks are handed off"* and
*"i basically want to continue everything as it is right now but on a different
chat and let it work for 3 hours but we need a perfect handoff"*.

**The working tree is clean.** Nothing is half-applied in `src/`, `scripts/` or
`docs/`. Everything below is a start, not a rescue.

### Done, verified, committed

- logged DJ Burns and Nate, 30 min each, 2026-09-06 — read back from the database
- the Weight Room tab (grid · due strip · available row · what the room did)
- the replan, written against his sheet and served at :4180
- the dashboard: 7 cards → 3, nothing empty on screen
- contact minutes → density % → Q1–Q4, proven end to end and restored
- the printed block: two columns, days flow, EXPO black masthead
- the host at :4181 leading with live production beside this branch
- this handoff, and `scripts/audit-handoff.mjs` which checks it ten ways

### 1 — Athlete-facing section on the host  ·  READY TO WRITE

His words: *"add another section: chances that touch the athlete
ui/expeiernce/portal and also show before after"*.

**The screenshots already exist** — captured, on disk, not yet rendered:

```
audit-out/pairs/portal-he-live.png     audit-out/pairs/portal-he-branch.png
audit-out/pairs/pt-zone-he-live.png    audit-out/pairs/pt-zone-he-branch.png
audit-out/pairs/pairs-he.json          (the manifest the page reads)
```

**What to do:** in `scripts/build-tonight.mjs`, add a section that reads
`pairs-he.json` the way the existing one reads `pairs.json` (the `LIVE` const
and `LIVE_TITLES` map are right at the top). The copy that belongs with it:

- On production an athlete who picks Hebrew **still sees English** — the portal
  renders outside the language provider. On this branch it renders Hebrew.
  That single fix is the largest athlete-visible change in the branch.
- Dates: `27th of August 2026` → `27 באוגוסט 2026`. Order never changes.
- Translated tonight and in the earlier block: the install prompt, meal log,
  messages, PRs, history headings, the mid-session words, READINESS GRAPH, NOTE.
- Nothing an athlete *does* changed: no new data is asked of them, and
  `readinessAutoreg.js` — which the portal shares — was not touched.

**Verify:** rebuild, then screenshot the page and LOOK at it —
`node scripts/shoot.mjs http://127.0.0.1:4181/ audit-out/tonight-look.png 1400 1000 full 6000`.

### 2 — Align the pairs for comparison  ·  DESIGNED, NOT BUILT

His words: *"make sure everything on the localhost is aligned right to left for
easy choice of before and after"*.

Both panes of a pair should sit at the same scale, start at the same point, and
**scroll together**, so his eye can move between before and after without
hunting. In `scripts/build-tonight.mjs`:

- give the two `.shot` panes a fixed equal height and `overflow:auto`
- add a small inline script that mirrors `scrollTop` between the two panes of
  each `.pair` (guard against the feedback loop with a flag)
- keep images at `width:100%` so both render at the same scale

### 3 — Refresh the host at the END of the next block

His instruction was *"when youre done update the local chrome host to be
current"* and *"but only after 3 hours have past"*. So: work first, then

```
node scripts/shoot-prod-vs-branch.mjs                     # English pairs
LANG_APP=he JOBS="athlete:/athlete:portal,pt:/coach/bhbc:pt-zone" \
  node scripts/shoot-prod-vs-branch.mjs                   # Hebrew pairs
node scripts/build-tonight.mjs
node scripts/build-bhbc-replan.mjs
node scripts/build-recap.mjs
```

The three servers are started with `scripts/serve-page.mjs` / `serve-recap.mjs`
and **die when the shell that started them exits** — check them with a plain
`http.get`, not `curl` through `execSync` (cmd.exe eats `%{http_code}`).

### 4 — Before/after for EVERY entry in the full recap  ·  he said "FOR LATER"

The recap at :4179 lists every undeployed commit with screenshots of the result
only. He wants a before beside each. Historic "befores" do not exist; the
honest version is what :4181 now does — live production against the branch, per
surface. Extending that to every surface in `docs/SURFACES.md` is the job.

### 5 — Replan items deliberately not built yet

| Item | Why it is parked |
| --- | --- |
| S&C minutes per day (`8 Min + 6 Min` in his sheet) | needs a per-day S&C field; the plan text already has a home |
| Basketball Plan A/B on the day | session plans already carry it; no second place needed until he asks |
| The month grid on the dashboard | it would re-introduce the duplication this replan removed — the board carries LAST LIFT instead. **Ask him** (open question 5) |

### 6 — Standing chores

- Marketing (`expo-il/`) and demo parity after any user-visible change.
- `docs/SURFACES.md` lists every route/tab; a new tab belongs in it. **The
  Weight Room tab is not in it yet** — add it.

## 13 · Traps already paid for — do not pay again

1. A probe that clicks tabs by **English labels** measures one tab five times
   when the app is in Hebrew. Click by **index**. Identical word counts on every
   line is the tell.
2. Clearing only `sessionStorage` leaves the previous seat signed in — the
   Supabase session lives in `localStorage`.
3. Grepping source for an on-screen word fails when **CSS uppercases** it: the
   DOM said `Update`, the screen said `UPDATE`. Dump the element markup instead.
4. The club zone has **its own** language switch (the `עב` button). Setting
   `expo-lang` does not move it.
5. `git log --format=%h|%s` through node's `execSync` runs in **cmd.exe** and the
   pipe eats the format. Quote it.
6. The Bash tool's heredocs **mangle backslashes** — write patch scripts with the
   Write tool.
7. A `file://` screenshot of a PNG **hangs** the shooter for the full timeout.
8. **Lazy-loaded images** never render in a full-page capture.
9. `node` resolves `/tmp/...` to `C:\tmp` — use the scratchpad path.
9a. **gviz is lossy on this spreadsheet.** `gviz/tq?tqx=out:csv&sheet=NAME`
    downloads a file, but on a sheet full of merged cells it comes back with
    most cells empty (`headers=0` does not help). Export by `gid` instead.
9b. **A tab click that does not switch tabs is silent.** One run read the same
    gid for all five tabs and overwrote every month with a copy of February.
    The gids are pinned in the downloader now, and it shouts if two months
    come back byte-identical.
10. A script run from outside the repo cannot resolve `puppeteer-core` — keep
    probes inside the repo.
11. The service worker reloads the page after a rebuild: "Attempted to use
    detached Frame" means *retry*, not *broken*.
12. Anything that writes to the store must snapshot to `audit-out/bhbc-state/`
    and restore.

## 14 · Where everything lives

```
src/BhbcView.jsx            the club zone (tabs, boards, modals)
src/bhbcHe.js               its Hebrew, composed not translated
src/dates.js                fmtPrettyDate / fmtCompactDate / fmtNumericDate
src/readinessAutoreg.js     readiness headlines — SHARED with the athlete app. NOT touched tonight; it does differ from master (+96 lines from an earlier session on this branch)
src/themes.css              @media print rules for the block export
src/PlansView.jsx           the printable block markup
docs/SURFACES.md            every route/tab; audits enumerate from it
audit-out/bhbc-sheet/       his five month tabs as CSV
audit-out/bhbc-state/       restore points for every store write
audit-out/pairs/            live-vs-branch screenshots + pairs.json
scripts/verify-*.mjs        118 gates
```

## 15 · Known gaps and honest caveats

- The **demo probe** (`audit-out/probe-demo-dates.mjs`) carries a header saying
  it is **not trustworthy yet** and must not be cited as passing. `/demo/athlete`
  renders correctly by hand check; the probe's numbers do not agree with that.
- The weight-room grid stretches its day columns to fill the card; with only six
  days on screen the squares sit far apart. It tightens as the month fills.
- "Nothing written" appears three times in *what the room did* because no
  session plans exist for those days yet. It is a prompt, not a defect.
- The exercise title **"Continious"** (should be Continuous) appears 10 times in
  his library data. **Not edited** — library data is never bulk-touched without
  his explicit OK.
- `verify-prod-current` is red by design and goes green only on deploy.

---

## Audit log — ten passes over this document

Run it yourself: `node scripts/audit-handoff.mjs`. It re-derives every claim
it can from git, disk, the database, the sheet CSVs and the live hosts, writes
`docs/handoff-audit.md`, and exits non-zero if anything does not match. It is
meant to fail loudly.

| # | Pass | What it checks |
| --- | --- | --- |
| 1 | Git facts | branch, HEAD sha, commits ahead, and the file/insertion counts printed above |
| 2 | Paths | every `src/` `scripts/` `docs/` `audit-out/` path named anywhere in this file exists (30 of them) |
| 3 | Scripts | every script named documents itself with a header and a usage line |
| 4 | Hosts | :4179, :4180, :4181 answer 200 |
| 5 | Database | roster, fixtures, `contactMin` restored to zero, the lift totals — AND that this document states those numbers |
| 6 | Memory | the three memory files exist, tonight is indexed first, the "bbq" trigger survives |
| 7 | Gates | the club-zone and athlete gates (run separately, results below) |
| 8 | Sheet | every value quoted in §5 is really in the CSV; the five months are five DISTINCT files; the note-lane counts match |
| 9 | Code | the claimed behaviour is really in the source, and `readinessAutoreg.js` was untouched tonight |
| 10 | Open questions | nothing from this branch is on master; the open items are still recorded; the demo probe still carries its warning |

### What the audit caught in the first draft of this file

Four claims were wrong, and the audit is the only reason they are not still
sitting here:

1. **"428 files changed."** That was `git diff --stat` scoped to `src` and
   `scripts`. The whole tree is **510 files, +88,163 / −4,270**.
2. **"The note lanes are almost always empty."** Counted: 45 filled cells
   across the five months, and January — which has the fewest note columns —
   is the most annotated. Sparse, not empty.
3. **"February dropped the PT lane."** January dropped it too, and earlier.
4. **"`readinessAutoreg.js` untouched."** True of tonight; it differs from
   master by +96 lines from an earlier session.

It also caught something worse in the evidence itself: the five month CSVs had
been downloaded through **gviz by sheet name**, which is lossy on this file —
August came back with nearly every cell empty. Every quote in §5 had been read
from a different, complete export. The CSVs are now true exports by pinned gid,
and the downloader shouts if two months come back byte-identical (one run had
written February into all five files).

### The audit was itself broken on purpose

A gate that has never failed has proven nothing. Three false claims were
planted in this file and the audit re-run:

| Planted lie | Caught? |
| --- | --- |
| "511 files changed" | yes — pass 1 |
| "August: 4 filled note cells" | yes — pass 8 |
| "roster 11" | **no** — the hole is described below |

Pass 5 was reading the database and printing the truth, but never checking that
this document said the same thing. It cross-checks the prose now, and the same
planted "roster 11" fails it. That hole existed because verifying reality and
verifying a document about reality are two different jobs.

### Gate results (pass 7, run separately)

| Gate | Result |
| --- | --- |
| `verify-bhbc-collapse` | 0 — every box collapses and stays collapsed |
| `verify-pt-no-backend` | 0 — the club zone holds up with no backend |
| `verify-athlete-no-backend` | 0 — the portal holds up with no backend |
| `verify-athlete-journey` | 0 — the athlete journey runs clean (real seat) |
| `verify-mobile-overflow` (owner + pt, 390px) | 0 — nothing is cut off |
| `verify-no-raw-values` (owner, athlete, pt; en + he) | 0 — nothing internal reaches the screen |
| `verify-hebrew` | 0 violations |
| `verify-marketing-site` | 0 — 54 route/width/language combinations |
| `verify-prod-current` | red by design; goes green only on deploy |

---

## 16 · The prompt to start the new chat with

Paste this, and nothing else, into a fresh conversation in this repo:

```
Read docs/HANDOFF-2026-09-06.md end to end, then MEMORY.md and
project_bhbc_replan_2026_09_06.md, then git log --oneline -20. That is the
state; do not re-derive it.

Then work nonstop for 3 hours on the queue in section 12 of that handoff, in
order: the athlete-facing section on the local host, then aligning the pairs so
before and after line up, then the rest of the queue. Do not deploy anything.
Refresh the three local hosts at the END of the three hours, not before.

Rules that matter most here: never test on the live site; snapshot before any
write to the store and restore after; never invent training data; verify from
the real seat and LOOK at the screenshot before saying something is done.
```

Everything that prompt refers to is in this file, and every claim in this file
is checked by `node scripts/audit-handoff.mjs`.
