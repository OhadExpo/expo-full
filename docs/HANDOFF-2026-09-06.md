# SESSION HANDOFF — 2026-09-05 → 09-07

## What this is, and what it covers

The handoff for **this entire working session**, written to be read cold by a
conversation that has none of it in context.

| | |
| --- | --- |
| **Covers** | every instruction given in this session and the block before it — **56 of them, in §14**, each with its status |
| | the 09-07 shift (§4b): the performance work, the video facade, the private browser |
| | the BHBC replan (§5–8), which was the largest single piece |
| | the athlete-facing work: the portal Hebrew, dates, translated labels (§8, §11) |
| | the printed block redesign (§8, §12) |
| | the local hosts and before/after pages (§15) |
| | the gates, probes and their traps (§13) |
| **Does not cover** | anything from earlier nights. Those live in the memory files listed in §17 — read `project_handoff_2026_09_06.md` and the ones it points to for the offline work, the revenue recovery and the Hebrew coverage pass |
| **Does not include** | a deploy. Nothing on this branch is live (§3) |

The BHBC replan gets the most pages because it was the most work, not because
this is a BHBC-only document.


Merged from ten separate handoffs written from ten different vantage points
(`docs/handoff/01…10`), because each one catches what the others miss. Audited
seventeen ways by `node scripts/audit-handoff.mjs`, which exits non-zero if any
claim here stops being true.

**His standing requirement for this file: nothing is ever lost.** §14 is a ledger
of every instruction he has given, verbatim, with what happened to it.

---

## 0 · The one word

**Say `pita` to a fresh chat and it resumes everything.**

It means: read this file end to end, then `MEMORY.md` and
`project_bhbc_replan_2026_09_06.md`, then `git log --oneline -20`; do not
re-derive the state; start at the top of the queue in §15 and work without
stopping; do not deploy.

His two older triggers still work: **`bbq`** and **`check my memory bro`** — read
memory + the newest handoff + `git log` from disk, then report state and the
open list.

By hand, in four minutes:

```bash
cat "C:/Users/Administrator/.claude/projects/C--Users-Administrator-Desktop-expo-full/memory/MEMORY.md"
cat "C:/Users/Administrator/.claude/projects/C--Users-Administrator-Desktop-expo-full/memory/project_bhbc_replan_2026_09_06.md"
cat "C:/Users/Administrator/.claude/projects/C--Users-Administrator-Desktop-expo-full/memory/project_handoff_2026_09_06.md"
git log --oneline -40
cat docs/HANDOFF-2026-09-06.md
```

---

## 1 · Orientation — you have never seen this repo

**EXPO** is a coaching platform Ohad built to replace a Google-Sheets business.
Vite + React + Supabase; Vercel deploys on push to `master`.

| Surface | Route | Who |
| --- | --- | --- |
| Athlete portal | `/athlete` | his ~20 private clients |
| Coach app | `/coach/*` | Ohad; Yuval on a staff tier |
| Club zone | `/coach/bhbc` | Bnei Herzliya BC — head coach, assistants, a physio |
| Marketing | `expo-il/` (own Vite root) | prospects |
| Demo | `/demo`, `/demo/coach`, `/demo/athlete`, `/try` | prospects, unauthenticated |

**Ohad** — solo S&C coach, עוסק מורשה, product owner, designer and QA. Blunt,
Israeli-direct. Wants deliverables, not menus of options. Push back when the
reasoning is wrong; he penalises flattery and unverified claims.

### The machine

- Windows 11. PowerShell primary; the Bash tool is Git Bash (POSIX).
- **Node's `execSync` spawns `cmd.exe`**, which eats `%` and `^` before the
  program sees them. Two audit passes failed on exactly this.
- Chrome runs on a **persistent debug profile**, port 9222. It stays signed
  into Google and into expo-app.co.il — never use a fresh or incognito
  profile, or every probe has to log in again. If nothing answers on 9222,
  kill Chrome and start it like this:

  ```bash
  "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\Users\Administrator\chrome-debug-budget" --no-first-run

  # is it up? and how many stray pages are open?
  curl -s --noproxy "*" http://127.0.0.1:9222/json/list | grep -c '"type": "page"'
  ```

  Scripts that connect and exit without closing their page leave tabs behind;
  32 of them once corrupted a timing-sensitive analysis.
- **The 9222 profile may belong to someone else.** On 2026-09-07 another
  Claude was driving it for a Google Pay support chat. Every browser script
  now takes `CDP=` and this shift ran on a PRIVATE instance:

  ```bash
  "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9223 --user-data-dir="%LOCALAPPDATA%\Temp\expo-probe-chrome" --no-first-run --headless=new about:blank
  CDP=http://127.0.0.1:9223 node scripts/perf-probe.mjs
  ```

  Before killing, resizing or clearing 9222, look at `/json/list` and ask.
- Scripts importing `puppeteer-core` must live inside the repo.
- `node` resolves `/tmp/...` to `C:\tmp`; use the session scratchpad.
- The Bash tool's heredocs mangle backslashes — write patch scripts with the
  Write tool.

### Running it

```bash
# eslint runs first and FAILS the build on any error
npm run build
# serves dist on 4173 — what every probe hits
npx vite preview
```

`http://127.0.0.1:4173` is this branch. `https://expo-app.co.il` is production,
a different and older commit.

### Seats — password `1234` everywhere, production included

| Seat | Email |
| --- | --- |
| owner | `ohadyproductions@gmail.com` |
| physio (BHBC) | `tomerlich11@gmail.com` |
| athlete, thin fixture | `diego@diegoday.com` |
| athlete, real history | `amit@enoshy.com` |

Thin data hides the defects gates look for. Point them at a real person with
`EMAIL=`.

### The thing that bites first

**The dev server shares the PRODUCTION database.** There is no staging copy.
Anything a local page or script writes lands in the store real athletes and the
club's physio read.

---

## 2 · The rules

Breaking one of these is the failure mode he penalises.

1. **Never build or test on the live site.** Branch, verify locally, **ask before
   any production deploy.** The only no-ask push to `master` is reverting an
   un-approved deploy of my own.
2. **Snapshot before any store write**, to `audit-out/bhbc-state/`, and read back
   after.
3. **Never invent training or financial data.** Blank beats wrong. No lift reads
   "never", not "0 days".
4. **Never use "cure", "diagnose" or "fix"** in UI strings, code comments or
   anything a client reads. Use "manage", "load management", "rehab strategy".
5. **Verify before declaring done**: restate the ask → verify the real path from
   the real seat → scope-check both ways → **look at the output** → name the gaps.
6. **Never `git add -A` / `git add .`** — stage named paths.
7. Hebrew is **written, not translated**: masculine singular, short, spoken
   Israeli. Exercise names stay English.
8. Israeli VAT 18%; VAT-inclusive → pre-VAT multiplier **0.8475**.
9. Marketing (`expo-il/`) and demo parity after every user-visible change,
   forever, without being asked.
10. **Never bulk-touch the exercise library.** One click once replaced 1,326 rows.
11. **Never reconstruct financial history into `bit_payment_requests`** — it is
    athlete-readable. Revenue lives in two owner-only tables.
12. Cue every task the moment he gives it, so he never repeats himself.
13. **Never ship a broken intermediate.** His words: *"you should never make
    mistakes at all, that's why we have rules"*. Do not narrate fixing your own
    parse error — use `node scripts/safe-edit.mjs <file> <patch.mjs>`, which
    patches, runs eslint and **rolls back automatically** if it breaks. Assert
    match counts; never splice JSX by index.
14. **`setViewport({width:390})` is NOT a phone** — desktop UA, DPR 1,
    `isMobile:false`. Every gate once read clean while his screen was broken.
    Mobile means real device emulation, and a clipping check cannot see
    WRAPPING. Use `scripts/lib/viewport.mjs`.
15. **Commit and push as you go.** Committing is part of finishing, not a
    step at the end: `git push origin bhbc-hebrew` (the BRANCH — never
    `:master` without asking). The branch is backed up as of 2026-09-07;
    `git rev-list --count origin/bhbc-hebrew..HEAD` should read 0 when you
    stop. Checkpoint constantly so a context limit cannot lose work.
16. **Keep `MEMORY.md` under ~24KB.** Over the ceiling it is silently
    TRUNCATED and a session never learns what fell off the end. It was 26,244
    bytes when this was written and had to be trimmed to 23,635 — every
    pointer kept, the hooks shortened.

---

## 3 · State

| | |
| --- | --- |
| Branch | `bhbc-hebrew` |
| HEAD | `d2d75de` (2026-09-11 evening) |
| Ahead of `origin/master` | **5 commits** — the athlete-portal remainder only: `ClientPortal.jsx`, `MealLogger.jsx`, `auth.jsx` (Hebrew login), the athlete tree inside the language provider in `App.jsx`, plus the compare modal's Hebrew labels and the 09-11 evening scripts and docs. (Against the stale LOCAL `master` the count is meaningless — see the rollback note below) |
| Deployed | **2026-09-11 19:45 — `deploy-0911` → `origin/master` = `cf884fb`** on his order ("deploy everything except athlete portal changes"). Coach app, club zone, marketing, demo, Hebrew audit, buttons, fonts, update-notice rules, compare redesign: live. The athlete portal's experience: held at what `dc80f2d` shipped. Verified: `verify-prod-current.mjs` PROD IS CURRENT, `prod-smoke.mjs` all routes clean, `/coach/bhbc` from the physio seat and `/athlete` from Diego's seat photographed on the new bundle (`audit-out/seat-physio-prod.png`, `audit-out/seat-athlete-prod.png`) |
| Restore point | `dc80f2d` — `git push origin dc80f2d:master --force-with-lease` undoes the whole 09-11 deploy |
| Previous production | `dc80f2d` (until 2026-09-11 19:45) |
| Local `master` | trails `origin/master` badly (1,078 behind at the time of writing) — **never** a rollback target |
| `origin/bhbc-hebrew` | **backed up** at `5cdcf3d` — pushed 2026-09-07 with his say-so. Keep pushing the BRANCH as work goes |
| Diff vs `origin/master` | **19 files** — the audit prints the exact current figures |
| Working tree | clean under `src/`, `scripts/`, `docs/` |

**Nothing ships without his yes.** That decision has been open since 2026-09-02.

> ### The branch IS backed up now
>
> `origin/bhbc-hebrew` exists as of 2026-09-07, at `5cdcf3d`. Before that push,
> every commit of this work lived on one disk with no copy — an adversarial
> review agent found that, not a gate.
>
> Pushing the BRANCH is a backup, not a deploy: only `master` auto-deploys, and a
> branch gets a Vercel preview URL production never sees. Verified after the
> push: `origin/master` is still `dc80f2d`, untouched.
>
> **Keep it that way — push the branch as the work goes:**
>
> ```bash
> git push origin bhbc-hebrew
> ```
>
> Check for drift with `git rev-list --count origin/bhbc-hebrew..HEAD` — it should
> be 0 when you stop for the night.

### If he says yes — the deploy procedure

Do not improvise this. His hard rule: **a reversible restore point exists
BEFORE the change, not after.**

```bash
# 1. record where production is NOW. Write this sha into the session notes.
# LOCAL master is NOT production. Fetch first, and take the target from the
# REMOTE. When this was written, local master trailed origin/master by
# origin/master (dc80f2d) - rolling back to the local one would have erased
# a month of live work.
git fetch origin
git rev-parse origin/master

# 2. the build must be green BEFORE it goes anywhere
# eslint gates it
npm run build

# 3. ship
git push origin bhbc-hebrew:master

# 4. wait for Vercel (8-15s), then PROVE it is live - never assume
# red until the new bundle is up, green after
node scripts/verify-prod-current.mjs

# 5. look at the real screens from the real seats
# the two columns should now match
node scripts/shoot-prod-vs-branch.mjs
```

**Rollback**, if anything is wrong: `git push origin <the sha from step 1>:master --force-with-lease`,
then re-run step 4. The sha from step 1 is `origin/master` — **never** local
`master`, which on this machine trails production badly. Reverting my own un-approved deploy
is the ONLY push to `master` that does not need to be asked for.

**There are TWO public deployments.** `expo-il/` is the marketing site on its
own domain with its own Vite root, and it has undeployed changes of its own.
Shipping the app does not ship it. Check `git status expo-il/` and treat it as a
separate decision.

**After a deploy**, check `/coach/bhbc` from the physio seat and `/athlete`
from a real athlete seat before saying it worked. A green build is not a
working feature.

---

## 4 · What happened tonight

1. *"today dj and nate worked out for 30 minutes each"* → logged. The roster has
   **two DJs**, so the tool refused to guess; he chose **DJ Burns (#30)**.
2. *"i want a display on bhbc of who worked out when (and what players need to
   workout soon)"* → built as a card, then, on his word, promoted to a tab.
3. **The pivot.** He sent his Google Sheet: *"that's litterally the main use of
   this table. wtf have we been doing up until now? plan a way better system im
   so not satisfied with what we currently have"*, then *"replan the entire bhbc
   system"*, *"and apply with way better thought and outcome"*, then *"also
   replan the entire dashboard of bhbc"*.
4. *"the pdf design is still too spacious and ugly, not branded enough"*.
5. *"show me on a local chrome host everything that's chnaged and undeployed…
   perfectly viewable and designed perfect for me to judge"*, then *"show me
   before and after for every thing"*.
6. *"can you create a full deatiled handoff… or is it too complicated at this
   stage and stuff will get lost?"* → this file. Nothing gets lost, because the
   state is on disk, not in a chat.

---

## 4b · What happened on 2026-09-07

1. *"i left a full handoff for you"* → resumed from this file; the queue in §15
   worked in order: the athlete-facing section and the mirrored pairs on the
   :4181 host (both done, `audit-out/verify-pair-scroll.mjs` 4/4).
2. *"make sure you finish every single task on the handoff. then search for
   leftovers, undone things, half-done things, and finish everything"* → the
   meal log's remaining English, the unread-notes banner, the warm-up heading
   and the bodyweight bidi line (§8); the rest of the leftovers are decisions
   in §15.6.
3. *"the app and the desktop expo and bhbc and all platforms have been very
   laggy and slow lately, check and fix it everywhere"* then *"maybe its just
   my wifi"* → measured from the real seats on production and the branch
   (`scripts/perf-probe.mjs`). It was not only the wifi — see §13b.
4. *"yuvi's videos take a while to load fix it"* → Yuval's block is YouTube
   links end to end and the portal mounted the full player per exercise. Now a
   poster and a tap (§13b).
5. *"make sure that you're not interferring with the google pay chrome chats"*
   → every probe moved to a private Chrome on 9223 (§1).

## 4c · What happened in the night of 09-07 → 09-08 (23:50 → the last 30 minutes)

He reset the timer twice (23:50, 00:22) and went to sleep. In order:

1. **His four lifts** (Zack Bryant 30, עמית מנחם 30, עמית גרשון 60, Nathan Knight 60 on 2026-09-07) — `scripts/bhbc-log-lift.mjs`, snapshot first, 4/4 verified, the weight-room tab reads *4 lifted today · 180 min*. Two roster names are stored in Hebrew now; `audit-out/list-bhbc-roster.mjs` prints them.
2. **Four reproducible before/after pairs** (tasks-autobody, dashboard-dashes, roster-offline, bw-bidi) in `scripts/build-before-after.mjs`; the recap at :4179 shows 13. The builder learned three things: the app language goes in AFTER sign-in, the install prompt is snoozed in storage, the coach-seat check reads Hebrew nav words.
3. **Looking at the dashboard pair found a real bug:** with the wifi up and Supabase unreachable the banner said OFFLINE while Collected MTD said ₪0 and three red toasts stacked. `unknown()` keys on `dataIncomplete` now; the five payment tiles dash; a failing LOAD is quiet behind the banner, a failing SAVE still toasts (in Hebrew when the app is); the athletes empty state says the server could not be reached.
4. **The last labels**: tasks placeholders / Auto-Alerts / Done (hotkeys re-anchored to `data-hotkey`), the BW tab, the floor chip, the day cards' exercise count, the messages composer, the PRs and history empty states, the notifications copy, SETS / REPS inside a workout. Every athlete tab now reads 1 Latin word (his name); inside a workout 5 (day + exercise names).
5. **Demo parity**: `/demo/athlete` rendered above the language provider (always English); wrapped, and `?lang=he|en` is honoured and kept, so expo-il.co.il can hand a Hebrew reader a Hebrew demo. The coach demo and `/try` have no translator — §15.4b item 4 is his decision.
6. **The intake form** spoke in slash forms (מלא/י, פרט/י, דרג/י); the instruction verbs are masculine now, the status choices stay.
7. **Sweeps on the final build**: 14 gates green (23:12–23:46), console 35 routes (one preview-only 404), light/dark parity on ten routes, phone width in Hebrew from three seats (0 cut off), four offline gates, the 22-route coverage sweep (§13e), the club zone tab by tab (roster 38 · schedule 40 · weight room 17 · medical 44 · games 159 — names, positions, league data; `audit-out/perf/bhbc-coverage-0908.log`).
8. **The drill-downs** (01:22–02:10): a route sweep counts the page a route lands on; `audit-out/probe-coach-drilldowns.mjs` opens a program (the editor: 609 → 312 Latin words), the Edit Athlete form (entirely English — both copies), the athlete detail page (195 → 127), the booking settings (18 → 5), and readiness wherever it renders. §8 has the files.
9. **Not deployed.** Every commit pushed to `origin/bhbc-hebrew` as it landed; the 14-gate battery re-ran on the final build at 02:13 (`audit-out/perf/gates-final-0908.log`).

## 5 · His sheet, read in full

**File:** `BHBC Schedule`, id `1sv3MyrRsmHkV5tdBkhtk55mxreGCoSIxC-HnCOmBNa0`.
**Tabs:** February, January, October, September, August (2025-26 season).
**Local copies:** `audit-out/bhbc-sheet/{August,September,October,January,February}.csv`
— TRUE csv exports, by pinned gid: August 1565508632 · September 536717987 ·
October 1828409130 · January 1985844099 · February 1343858464.
**Re-download:** `node audit-out/read-bhbc-sheet-all.mjs` (through the signed-in
debug Chrome; neither the Drive connector nor the mcp-gsheets service account
can see this file).

### The spine, row by row

| Row | Holds | 20/08/2025 |
| --- | --- | --- |
| Date | one column group per day | `20/08/2025` |
| Day | weekday | `Wednesday` |
| Time | session shape | `Early Practice` · `Late Practice` · `Double Day` · `OFF` in August; `GAME-DAY` from September on |
| Practice Time (MIN) | total minutes | `84 Min` |
| Contact Time (MIN) | contact inside them | `13 Min` |
| Density % | contact ÷ practice, banded | `15.5%` → `Low Intensity` |
| Total Available Players | headcount | `9 Players` |
| Basketball Notes | plan label | `Basketball Plan` / `Plan A` / `Plan B` |
| S & C Scale | quadrant + minutes | `Q1` · `12 Min` (also `8 Min + 6 Min`, `Warm-Up`) |
| S & C content (Feb) | free text | `- BW Strength - Dynamic Streching - Ladders` |
| Athlete × day cell | restriction code | `1`–`5`, or `OFF` |
| PT / S&C Notes | note lanes | counted below |

### His legends, unchanged in the app

- **Availability:** 1 No Restriction · 2 Minutes Restriction · 3 Non Contact ·
  4 Out–Medical · 5 Out–Personal. EXPO's `AVAIL` map already matched all five.
- **Density:** under 20% Low · 20–25 Moderate · 25–30 High · above 30 Very High.
- **Volume:** "Above 90 Min = High Volume".
- **Quadrants:** Q1 Low/Low · Q2 High volume, low intensity · Q3 Low volume,
  high intensity · Q4 *"Moderate"* volume, high intensity.

### The note lanes, counted rather than characterised

August, September and October carry TWO note lanes per day (PT and S&C).
January and February carry ONE (S&C only) — the PT lane was dropped between
October and January.

| Month | note columns | filled cells |
| --- | --- | --- |
| August | 24 | 1 |
| September | 60 | 11 |
| October | 50 | 9 |
| January | 22 | 17 |
| February | 22 | 7 |

Used, but thinly — 45 filled cells across five months of days × athletes. The
widest version (September, 60 columns) carries 11 filled cells; the narrowest
(January and February, 22 columns) carry 17 and 7. So the wide layout is not
the most used, and the narrowest month is the most annotated of all. Three
lanes a day made the grid unreadable and the PT lane did not survive the season.
That is his own design lesson: the grid is for **codes**; notes go one click deep.

*(An earlier draft called the lanes "almost always empty". The audit counted
them. Sparse, not empty — and January, the narrowest grid, is the most annotated
month.)*

---

## 6 · What the zone had instead, counted

On the live Overview before touching it (`audit-out/overview-now.png`):

- **Seven cards, ~2,150px** of scroll.
- **Next game** printed three times; **availability 8/2/0** three times;
  **medical** four times.
- **Never held a value:** Team Snapshot's Avg ACWR / Flagged / 7-day load; the
  board's ACWR (`· baseline` ×10), 7D (`—` ×10), the 14-day spark (blank ×10),
  Readiness (`no check-in` ×10).

All of those need an **sRPE per athlete per session** and a **wellness
check-in**. He collects neither.

## 7 · The principle now encoded

> Show what is maintained. Compute what can be computed. Nothing else earns a pixel.

**Maintained:** availability per athlete per day · practice and contact minutes ·
what the weight room did · medical status, onset, RTP · fixtures.
**Not maintained:** RPE per session · wellness check-in.

ACWR, readiness, monotony and strain stay in the product and appear **only when
their data exists** — `hasLoad` / `hasRead` in `LoadBoard` are that rule in code.

---

## 8 · Every change, file by file

### `src/BhbcView.jsx`

| What | Where | Why |
| --- | --- | --- |
| `WeightRoomTab` | new, rendered for `view === 'weightroom'` | his month grid |
| `NAV_TABS` | `['weightroom', tr('Weight Room')]` between Schedule and Medical | training before the record |
| Grid cell | tint = restriction, orange square = logged lift | one cell answers both questions |
| No future columns | `if (iso > today) break;` | half the board was empty future |
| `availPerDay` | counts cells with code 1 | his "Total Available Players" |
| `roomDays` | from **logged sessions**, not fixtures | a scheduled lift nobody attended is not a day in the room |
| Due strip | one line, longest gap first | replaced a card repeating all ten names |
| `DENSITY_BANDS`, `densityOf` | module level | his bands; `null` unless both numbers exist |
| `quadrant` | volume >90, intensity ≥25% | his Q1–Q4, derived |
| `contactMin` | fixture field + week-planner input (non-lift only) | the one new thing asked of him |
| `DensityBit` | today's chip and past practices | one component so thresholds cannot drift |
| Overview | S&C Brief and Next Game removed; Team Snapshot conditional; sessions panel merged into the report via `bare` | 7 cards → 3 |
| `LoadBoard` | `hasLoad`/`hasRead` gate four columns and the legend; `lastLift` column added | nothing empty on screen |
| Hebrew | RTP ladder, pain gate, referral line, availability pill, readiness headlines, `Update ›`, `1 record`, body parts | the physio reads this |

### The athlete-facing files — thirteen src files changed this session, not seven

| File | What changed, and why it matters |
| --- | --- |
| **`src/App.jsx`** | **The single most important athlete change in the branch.** The portal's early return rendered OUTSIDE `<LangCtx.Provider>`, so a client who picked Hebrew still got English — the Hebrew was already written and could never reach the screen. The fix wraps that return in the provider. On production this is still broken. |
| `src/SessionsView.jsx` | The floor grid printed a raw trainee id (`tr_ron`) where a name belongs. The fix takes a name map and falls back to a neutral label — **it deliberately does NOT resolve the name from the club roster**, because an EXPO athlete's identity must not leak into the club zone. |
| `src/TraineePRsView.jsx` | The session count moved OUT of the picker `<input>` onto its own line, after a measured mid-word clip at phone width. Putting it back inside re-breaks it. |
| `src/InstallAppPrompt.jsx` | Had no translator at all and spoke English to everyone. It mounts outside every provider, so it reads the language itself: `tr(readLang(), s)`. |
| `src/MealLogger.jsx` | Had no translator. Its error strings, day labels and totals are **still English** — deferred, and listed in §17. |
| `src/CoachMessages.jsx` | Had no translator; the athlete's messages tab now speaks Hebrew. |

### Others

- **`src/bhbcHe.js`** — ~90 new keys, composed in Hebrew, not translated.
- **`src/dates.js`** — `fmtPrettyDate` / `fmtCompactDate` turn the **month word**
  when `localStorage['expo-lang'] === 'he'`, read at call time in a try/catch.
  Order never changes. `fmtNumericDate` untouched. Bad input still passes through.
- **`src/ClientPortal.jsx` + `src/i18n.js`** — far more than two labels: the
  whole athlete-Hebrew wiring. A plan snapshot so the portal opens offline
  (`expo-plans-<clientId>`), an OFFLINE notice in `renderTopHeader()` so all six
  tabs carry it, the tab names, the bodyweight and history headings, the
  mid-session words, the warm-up flow, `READINESS GRAPH →` and `NOTE` — the
  arrow turning with the text. Many i18n keys; a duplicate key FAILS the
  build (`no-dupe-keys` is a build gate) and leaves dist stale.
- **`src/themes.css` + `src/PlansView.jsx`** — the printed block: two-column
  rows, no 266mm day floor, no 17mm row floor, days flow, EXPO black masthead
  with the mark on a white plate.
- **`docs/SURFACES.md`** — the zone's tab strip is listed, Weight Room included,
  with the note that a sweep must CLICK the tabs because they are not routes.

### Changed on 2026-09-07 — performance, video, the last Hebrew

| File | What changed, and why it matters |
| --- | --- |
| **`src/sw.js`** | CacheFirst runtime routes for the MediaPipe WASM (jsdelivr) and pose model (Google storage), and a stale-while-revalidate route for YouTube posters. Both MediaPipe files are cross-origin, so the precache never held them and they were fetched on EVERY coach page view that touched pose. |
| **`src/App.jsx`** | The auto-analyse warmer waits for 20s without input, a visible tab and a link reporting at least 3Mbps, and re-checks between clips. The club zone's fallback poll is 30s, not 5s; the realtime channel is what makes a change land instantly. |
| `src/DashboardView.jsx` | The auto-task rules read `plan_index` (weeks + day names, 11KB) instead of every plan's full `data` (475KB). The rules only ever used the last day's name. |
| `src/googleCalendarSync.js` + `index.html` | Google Identity loads on demand for the calendar connect, not as a 99KB script on every seat's every load. |
| **`src/VideoEmbed.jsx`** | `YouTubeLite`: poster frame + play plate until tapped; the tap mounts the real player with autoplay and pre-connects to YouTube's hosts. Used by `VideoEmbed` and both portal sites. |
| `src/ClientPortal.jsx` | The two YouTube iframes → the facade; the unread-notes banner and its link in Hebrew; the warm-up card title's word in Hebrew; the bodyweight unit isolated so "משקל · 84.2KG" stops rendering as "84.2 · משקלKG". |
| `src/WorkoutReview.jsx` | The next-clip prefetch (two hidden `preload="auto"` videos, 6MB average) mounts 8s after the workout opens and not on a link under 3Mbps or with save-data. |
| `src/MealLogger.jsx` + `src/i18n.js` | Every remaining English string in the meal log; the day label reads he-IL. |
| `public/bnei-herzliya-logo-w.png`, `public/logos/bhbc-logo.png` | 1146px crests rendered at 20–68px → 240px tall. 366KB → 115KB. Originals in `audit-out/logo-src/`. |
| 74 tracked `scripts/*.mjs` + `audit-out/*.mjs` | `browserURL` honours `CDP=`. |
| `src/DashboardView.jsx` + `src/App.jsx` (00:03, 09-08) | `unknown(rows)` keyed on `navigator.onLine`, which is TRUE on a wifi with no route to Supabase — the banner said OFFLINE while Collected MTD said ₪0 (seen in the dashboard-dashes pair). It keys on the App's `dataIncomplete` now, the banner's own signal, passed as a prop to the dashboard and the athletes page. |
| `src/DashboardView.jsx` (RevenueCard) | The five payment-derived tiles (30d / 90d collected, outstanding, avg LTV, avg ticket) read — when payments are unknown, like Collected MTD; MRR stays, it comes from the cached roster. |
| `src/TasksV8View.jsx` + `src/i18n.js` (00:1x) | The last labels: Add a task… / Search tasks… / Search… placeholders, the Auto-Alerts section name and count line, the Done footer, the quick-add placeholder. The two hotkeys found their inputs by ENGLISH placeholder — they select by `data-hotkey` now, or a translated placeholder would have silently disconnected them. |
| `src/App.jsx` + `src/i18n.js` (01:30, 09-08) | **Demo parity.** `/demo/athlete` (the REAL portal in demo mode) returned above `<LangCtx.Provider>` — the same bug the real portal had on 09-06 — so a Hebrew reader met an English demo; wrapped. `readLang()` honours `?lang=he` / `?lang=en` and keeps it, so expo-il.co.il (another origin, its storage out of reach) can hand a Hebrew reader a Hebrew demo. Measured: fresh 107 Latin words / LOG OUT → with `?lang=he` 88 / יציאה · בלוק · שבוע, kept on the next visit, back with `?lang=en` (`audit-out/probe-demo-lang.mjs`). |
| `src/intakeFormSchemas.js` + `src/ClientPortal.jsx` (01:00) | The Hebrew intake form spoke in slash forms (מלא/י, פרט/י, דרג/י, כבד/ה) — the register rule is masculine singular; the instruction verbs are masculine now. The status CHOICES (רווק/ה, נשוי/אה) were left as they are: a woman picking "נשוי" would be entering a wrong fact, and that is his call. The athlete day cards say תרגילים, not EX. |
| `src/PlansView.jsx` + `src/i18n.js` (01:25–01:31) | **The program editor** — the route sweep only ever listed programs; `audit-out/probe-coach-drilldowns.mjs` opens one and found the coach's main tool half in English (609 Latin words). ← BACK, MORE, EDITOR/OVERVIEW, Program Name / Phase / Weeks, PATTERN COVERAGE, WARM-UP, the two column-header rows, DAILY, COLLAPSE/EXPAND ALL, the picker's Exercise label, CLEAR / CLEAR ALL / ↩ LIBRARY, Update the exercise database / Save new exercise, the overview's EX · SETS. Now 312, all exercise and plan names; the taxonomy chips (HIP HINGE…) stay English by rule. The athlete edit form (127: e-mails) and the portal preview (36: exercise names) were measured on the same pass. |
| `src/ClientPortal.jsx`, `src/TraineePRsView.jsx`, `src/PushToggle.jsx`, `src/CoachMessages.jsx` (01:10–01:20) | The athlete's other five tabs and the inside of a workout, measured per tab (§13e). |
| `src/TraineesView.jsx`, `src/TraineeDetail.jsx`, `src/ui.jsx`, `src/BookingView.jsx`, `src/ReadinessRow.jsx`, `src/CheckinTrends.jsx` (01:40–02:10) | **The drill-downs the route sweep never sees.** The Edit Athlete form (both copies: the roster modal and the detail page's) was entirely English in Hebrew — title, every label, add-email, archive/cancel/update/create; the booking settings panel (header, slug, save, weekday names: 18 → 5); the athlete detail page's action buttons, back, vitals header, payment tiles, show/hide all, week; readiness (PAIN / SLEEP / ENERGY and the scale words) everywhere it renders — the shared row, the detail history, the review, the trends card. Detail page 195 → 127 Latin words; what remains is the coach-log / evaluation cards' labels (COACH LOG, ACTIONS, ACTIVITY, EVALUATION, RECENT, CURRENT, LAST, DATE, NEW) — next shift, they live in sub-components. |
| `public/heebo-fonts.css` + `src/i18n.js` (`tbFor`/`useTB`) + 14 views (09-11 13:40–14:30) | **Buttons the same size in Hebrew as in English, everywhere** (his ask). Two causes, measured by `audit-out/probe-button-parity.mjs` (414 differences on 18 routes): Heebo's line box was 160 per 100px against Nord's 120 (`audit-out/probe-font-metrics.mjs`), so every Hebrew label made its button 4–6px taller — pinned with ascent/descent/line-gap overrides (scaled by the existing 109% size-adjust); and a shorter Hebrew word shrinks a text-sized button — a button/tab label now stacks both languages in an inline grid, one invisible, so the box is the wider of the two in BOTH languages (the coach nav, the hub tabs, the flagged action buttons). |
| `src/SwUpdateBanner.jsx` + `src/ClientPortal.jsx` (workout flag) | **The update notice, with rules** (his ask: athletes got it at every login). Nothing in the first 12s of a load; a non-blocking top pill with UPDATE / LATER (24h snooze); still auto-applies when the tab is hidden or after 60s idle; never mid-workout (`window.__expoWorkoutActive`, set by the logger), mid-upload or while filming; once per bundle; the old blocking modal only after 3 ignored days. Hebrew copy through the dictionary. |
| `src/ClientPortal.jsx` + `src/TrySandbox.jsx` (09-11 13:35) | **No FOCUS anywhere in the athlete portal** (his ask, and the 09-02 ruling): the per-exercise line under the exercises is gone, the logger no longer prints weekly-focus text as COACH'S FOCUS (a form-video note is labelled FROM YOUR COACH), the sandbox mock's focus card and lines too. |
| `src/WorkoutReview.jsx` (compare, 09-11 17:15) | **Compare mode redesigned** (his three asks): one transport bar for BOTH videos, centred under them — step · PLAY BOTH / PAUSE · step, the speed as a segmented control, LOOP, the two SYNC directions as a pair; each side keeps only SKELETON / REPS / FULL; METRICS is not a compare-mode question and is gone from it; titles centred above their video; Space plays/pauses both, ←/→ step both. VS DEMO is now the fallback of the same decision: earlier reps of this exercise → COMPARE, none → VS DEMO. |
| `src/coachNotes.js` | `reportFailure`: a network failure on a LOAD no longer toasts — the banner is that notice; the same action failing the same way toasts once a minute, not once per retry (three stacked red boxes over the Messages card were the symptom). A network failure on a SAVE / update / delete / link still toasts — the coach just did that and must hear nothing was saved — and in Hebrew when the app is ("שמירת המשימה נכשלה — אין חיבור לשרת…"). Refined 01:15 after reading the callers: the first cut had silenced saves too. |
| `src/TraineesView.jsx` + `src/i18n.js` | The athletes empty state says the server could not be reached (when `dataIncomplete` and no rows) instead of inviting a first athlete; both empty states through the dictionary. |
| `src/SessionsView.jsx`, `src/BugsView.jsx`, `src/SmartImportView.jsx`, `src/WaitlistView.jsx`, `src/WorkoutsView.jsx`, `src/WorkoutReview.jsx`, `src/ChatAuditView.jsx` (22:5x–23:4x) | The last English labels on the small coach routes (§13e has the counts). |
| `src/autoTaskHe.js` (new) + `src/TasksV8View.jsx` + `src/taskFormat.js` | Auto-task bodies re-said in Hebrew at render; stored rows untouched. |
| `scripts/build-before-after.mjs` | Four more reproducible pairs (tasks-autobody, dashboard-dashes, roster-offline, bw-bidi), a `clickText` step, the app language set AFTER sign-in (the sign-in helpers know the English login only), the install prompt dismissed, the coach-seat check reads Hebrew nav words. |

### Scripts added

| Added 09-07 night → 09-08 | What it is for |
| --- | --- |
| `audit-out/probe-coach-drilldowns.mjs` | Latin words on the coach's drill-downs in Hebrew: opens a program, the Edit Athlete form, the portal preview; `DETAIL=1` adds the athlete detail page and the review list, `MORE=1` four more. A route sweep never sees these. |
| `audit-out/probe-athlete-tabs.mjs` · `audit-out/probe-athlete-workout.mjs` | Diego's seat in Hebrew: every portal tab counted; a started workout and its first exercise. |
| `audit-out/probe-demo-lang.mjs` | Does `/demo/athlete` follow the language, does `?lang=he` take and keep. |
| `audit-out/list-bhbc-roster.mjs` | Read-only roster print, because two names are stored in Hebrew now and `bhbc-log-lift.mjs` matches on the stored name. |
| `audit-out/reload-tab.mjs <fragment>` | Reloads the matching tab in the debug Chrome (9222) after a page rebuild. |
| `audit-out/chain-final-0322.sh` | The last 30 minutes: every pair re-shot (login, 16 + small + editor + form + detail + demo), the three pages, the mirror check, his three tabs reloaded, one screenshot per page. |
| `scripts/build-before-after.mjs` (+4 pairs, `clickText`, language after sign-in, install prompt snoozed) · `scripts/shoot-prod-vs-branch.mjs` (the 4th JOBS field can open a card, not only a tab) | |


| Script | Does |
| --- | --- |
| `scripts/bhbc-log-lift.mjs` | `LIFTS="name:minutes,…" DATE=…` — logs lifts, refuses ambiguous names, snapshots, reads back |
| `scripts/build-bhbc-replan.mjs` | the replan page |
| `scripts/build-tonight.mjs` | the before/after page |
| `scripts/serve-page.mjs` | serves any page on its own port with the app's fonts |
| `scripts/shoot-prod-vs-branch.mjs` | screenshots live production and the branch, same seat/route/width, `LANG_APP=he` aware |
| `scripts/audit-handoff.mjs` | checks this document fifteen ways |
| `audit-out/read-bhbc-sheet-all.mjs` | downloads every month tab by pinned gid |
| `audit-out/analyse-bhbc-sheet.mjs` | prints the sheet's structure and typed notes |
| `audit-out/test-density.mjs` | end-to-end density test **that restores the data** |
| `audit-out/probe-bhbc-coverage.mjs` | Latin words per club-zone tab in Hebrew |
| `audit-out/probe-athlete-coverage.mjs` | the same for the athlete portal |
| `audit-out/shot-weightroom.mjs` | screenshots any zone tab, any seat, any language |
| `scripts/verify-handoff-full.mjs` | walks every line of this handoff and the ten lens files |
| `scripts/break-test-audit.mjs` | plants one lie per audit pass and restores everything after |
| `scripts/verify-no-raw-values.mjs` | no ids, NaN, undefined or Invalid Date on any screen; takes `LANG_APP=` |
| `scripts/bhbc-log-backlog.mjs` | logged the practice backlog; attendance derived from medical dates, never invented |
| `scripts/build-before-after.mjs` | reproduces before/after pairs by rebuilding the OLD code — nine pairs in `audit-out/beforeafter/` |
| `scripts/gate-board.mjs` | runs the battery and writes `audit-out/gate-board.json` (last: 60/61) |
| `scripts/safe-edit.mjs` | patch + eslint + automatic rollback — the tool for never shipping a broken intermediate |
| `scripts/shoot-plan-pdf.mjs` | renders the printed block to `audit-out/pdf/` and screenshots its pages |
| `audit-out/probe-bhbc-english-where.mjs` | dumps the markup of whatever is still English on a tab |
| `scripts/perf-probe.mjs` | bytes, requests, long tasks, time-to-content per seat/route, cold and warm; `BASE=` picks production or the branch; writes `audit-out/perf/<tag>.json` |
| `audit-out/probe-yt-portal.mjs` | what opening a step costs from an athlete seat (Diego): iframe or facade, YouTube bytes |
| `audit-out/probe-yt-embed-cost.mjs` | one bare YouTube embed versus one poster, in a blank page |
| `audit-out/verify-pair-scroll.mjs` | proves both panes of every pair on :4181 mirror each other's scroll |
| `audit-out/chain-close-0907.sh` | the close-of-shift chain: gates, pairs, pages, on the private Chrome |

---

## 9 · The data, exactly

Supabase table `store`, key/value JSON, read with the owner seat signed in.

```js
const get = async (k) => (await sb.from('store').select('value').eq('key', k).maybeSingle()).data?.value ?? null;
```

| Key | Shape | Tonight |
| --- | --- | --- |
| `expo-bhbc-roster` | array of athletes | 10 |
| `expo-bhbc-loads` | object keyed by athlete id | 10 |
| `expo-bhbc-fixtures` | array | 33 |
| `expo-bhbc-medical` | object keyed by athlete id | 8 |
| `expo-bhbc-plans` | object keyed by `date\|start` | 1 |

**roster[i]** — `id, name, jersey, position, nationality, dob, age, height,
heightCm, weight, team, format, goals, status, arrival, startDate, injuries`

**loads[athleteId]** — `{ loads, sessions, readiness, attendance, availability }`

```json
sessions["2026-09-06"][0] = {
  "by": "ohadyproductions@gmail.com",
  "min": 90, "rpe": null, "load": 0,
  "team": true, "type": "Practice", "start": "16:00", "attended": true
}
availability = { "2026-08-19": 1, "2026-08-23": 1 }
```

`type` on a LOGGED row is the capitalised label a coach picked — `Practice`,
`Lift`, `Shootaround`, `Game`. On a FIXTURE it is lowercase — `practice`,
`lift`, `game`. Code matching them must map between the two; `PastPractices`
holds the canonical helper.

**medical[athleteId].injuries[0]** — `{ by, id, pain, side, type, notes, status,
bodyPart, progress[], onsetDate, rtpTarget, … }`. `status` is
`available | limited | non-contact | out` and sets a FLOOR on availability: a
coach can make a day worse than the medical fact, never better.

**fixtures[i]** — `{ date, type, start, end, minutes, optional }` plus, new and
optional, **`contactMin`**. Absent means no density is shown. Written only by
the week-planner editor.

**plans[`${date}|${start}`]** — `{ focus, plan, updatedAt }` — where "what the
room did" gets its words.

### Non-negotiable data rules

1. **RPE is never invented.** Every gym row this project writes carries
   `rpe: null, load: 0`. 78 lift sessions are stored (72 at the 09-06 handoff, four more on 09-07 evening, two on 09-11); not one has a fabricated
   RPE.
2. **Snapshot before any write** and read back after — `scripts/bhbc-log-lift.mjs`
   and `audit-out/test-density.mjs` both do it; copy the pattern.
3. **Never derive a per-client amount from the roster sheet.** It holds a rate
   and sessions-since-payment, which is not what was paid.
4. **Revenue is owner-only** — `revenue_sheet_event`, `revenue_month_total`.
   `bit_payment_requests` is athlete-readable. `scripts/verify-revenue-private.mjs`
   proves the isolation from five seats.

---

## 10 · Data operations performed on production tonight

| When | What | Restore point |
| --- | --- | --- |
| 18:26 | Nathan Knight, 30 min Lift, 2026-09-06 | `audit-out/bhbc-state/loads-before-lift-2026-09-06-1826.json` |
| 18:27 | DJ Burns, 30 min Lift, 2026-09-06 | `…-1827.json` |
| 09-07 23:53 | Zack Bryant 30, עמית מנחם 30, עמית גרשון 60, Nathan Knight 60 — min Lift, 2026-09-07 ("today zach, amit menachem worked out for 30 minutes, and gershon and knight worked out for 60 minutes (weight room all)"). Two of the four are stored in HEBREW on the roster now, so the name given to `bhbc-log-lift.mjs` must be the Hebrew one; `audit-out/list-bhbc-roster.mjs` prints the roster | `audit-out/bhbc-state/loads-before-lift-2026-09-07-2353.json` |
| 09-11 13:30 | עמית גרשון 60, Nathan Knight 60 — min Lift, 2026-09-11 ("today amit gershon lifted for 1 hour, and nathan for 1 hour"), 2/2 verified | `audit-out/bhbc-state/loads-before-lift-2026-09-11-1330.json` |
| 09-05 22:11–22:18 | **the practice-attendance backlog** — 176 session entries across 20 practices and 2 lifts, attendance derived from injury onset → RTP dates, `rpe: null, load: 0`. The loads key went from 58 to 118 entries. Written by `scripts/bhbc-log-backlog.mjs` (idempotent) | six `audit-out/bhbc-state/*-20260905.json` |
| 19:2x, 22:1x and later | **three** density trials, not one — each wrote a single `contactMin` through the real editor and restored the whole key afterwards. The last ran hours after the first; check the timestamps rather than assuming a window | one `audit-out/bhbc-state/fixtures-before-density-*.json` per run |

**Verified now:** roster 10 · fixtures 33 · **0 fixtures with `contactMin`** ·
78 lift sessions. Newest two: 11-09 עמית גרשון 60, 11-09 Nathan Knight 60 (restore
point `audit-out/bhbc-state/loads-before-lift-2026-09-11-1330.json`); then 07-09 Zack Bryant 30, 07-09 עמית מנחם 30, 07-09
עמית גרשון 60, 07-09 Nathan Knight 60; before them 06-09 DJ Burns 30, 06-09 Nathan
Knight 30, 05-09 Daeshon Francis 30, 05-09 Nathan Knight 60. 1 Sep is 9 lifters × 12 min = 108,
which is exactly what the new "what the room did" line prints.

---

## 11 · What the people on the other end will see

### The physio (`tomerlich11@gmail.com`, reads Hebrew)

**Before:** switching the zone to Hebrew still left his Medical tab carrying
**147 Latin words** — the whole RTP ladder, the pain gate, the referral line,
the availability pill, every readiness headline, `Update ›`, the record count,
and the body part on each squad card.

**Now:** 45, and every one is a name or shorthand a coach reads as English
anyway (PPG, MD-).

**Must not lose:** he works with no backend. `verify-pt-no-backend.mjs` must
stay green.

### The athlete (~20 of them)

**The change that matters:** on production an athlete who picks Hebrew **still
sees English** — the portal renders outside the language provider, so the Hebrew
that was already written never reached it. On this branch it renders Hebrew.
That is the largest athlete-visible difference in the branch.

Also: dates read `27 באוגוסט 2026`; the install prompt, meal log, messages, PRs
and history headings speak Hebrew; the mid-session words were translated.

**Nothing new is asked of them.** `src/readinessAutoreg.js`, which their portal
shares with the club zone, was not touched tonight — the club-zone translation
happens at the render.

**Must not lose:** `verify-athlete-no-backend.mjs` and `verify-athlete-journey.mjs`.

### Coaches

Same zone in a restricted form (`asCoach`): no Sessions tab, boards read-only.
The Weight Room tab is visible to them; it is a board, not a control.

### Prospects

`verify-marketing-site.mjs` passes 54 route/width/language combinations.
`/demo/athlete` renders the real portal, so the Hebrew fix reaches the demo too.

---

## 12 · Design — the taste rules and the scars

**EXPO:** dark `#0a0a0b`, cyan `#39BDFF`. The cyan never deepens.
**Bnei Herzliya:** the zone is **always light**. Navy `#1E3D74`, deep navy
`#14294F`, orange `#F26A2B`, deep orange `#D9541A`.

```js
const TOKENS = {
  '--c-ac': NAVY_DEEP,
  '--c-stripBg': NAVY_DEEP,
  '--c-cardBd': 'rgba(30,61,116,0.26)',   // a NAVY hairline, not a mix with EXPO's
  '--c-bd':     'rgba(30,61,116,0.22)',
  '--bhbc-amber-text': '#8A6410',          // the LIGHT-page amber, pinned
};
```

Two scars in that block: mixing 20% navy into `--c-bd` left EXPO's cyan showing
through every card edge, which made a club popup read as an EXPO dialog; and
`--bhbc-amber-text` is declared twice on `:root`, so with EXPO in dark mode the
zone painted the dark-page amber on white — **2.15:1**, unreadable, on every
"limited" count. **Every BHBC modal carries these tokens**, because a modal is
portalled to `body`.

| Rule | Why |
| --- | --- |
| No icons or emoji — "just colors" | a triangle before every injured athlete was noise |
| Plain coloured text, not badges, in tight rows | badges add invisible padding |
| Measure the **ink**, not the box | he reported one pill 3×; the box was centred to 0.00px, the glyphs rode 0.6px high |
| Every card gets a strip header | `RefinedHeaderStrip` |
| Uniform strips: 13px, 0.04em, 14/14 | |
| Fixed-size toggles reserve the widest label | resizing reads as a flash bug |
| ≤0.5px borders do not render | screenshot-verify |
| Layout identical light/dark; only colour differs | |
| RTL: forward arrow `←` at the logical end; `‹` where `›` would be | |
| Hebrew sits +3px INSIDE the box | never taller boxes |
| The real font is **Nord** | `public/fonts/Nord-*.woff2`; CLAUDE.md is stale |

Decisions made tonight: the grid fills its card; future days are not columns;
the gap sits on the row and the due list is one strip; the chevron turns with
the text; Hebrew names beside Latin numbers need `unicode-bidi: isolate`; the
printed row is two columns with a cyan spine; the print masthead is a black band
with the mark on a white plate.

**What "too spacious" meant:** three rules added for the OPPOSITE complaint — a
266mm day floor, a 17mm row floor, and cues under the name across the full
width. Removing all three took Day A from ~1,400px to ~810px. If density is ever
complained about again, do not re-add a floor; look at what is actually empty.

---

## 13 · QA — what is proven, and how

118 `scripts/verify-*.mjs`; 34 touch BHBC. Most drive the real build in the
debug Chrome from a real seat.

> **Rebuild first, every time.** These gates drive `dist` through the preview
> server on 4173. Editing `src/` and running a gate verifies the PREVIOUS build
> and tells you nothing: `npm run build` first, and wait for the service worker
> to settle (a "detached Frame" error means retry, not broken).

**After touching the club zone**

```bash
node scripts/verify-bhbc-collapse.mjs
node scripts/verify-pt-no-backend.mjs
SEAT=pt W=390 node scripts/verify-mobile-overflow.mjs
SEAT=pt node scripts/verify-no-raw-values.mjs
LANG_APP=he SEAT=pt node scripts/verify-no-raw-values.mjs
node scripts/verify-hebrew.mjs
```

**After touching anything shared (dates, i18n, themes)**

```bash
node scripts/verify-athlete-journey.mjs
node scripts/verify-athlete-no-backend.mjs
SEAT=owner node scripts/verify-no-raw-values.mjs
LANG_APP=he SEAT=owner node scripts/verify-no-raw-values.mjs
SEAT=owner W=390 node scripts/verify-mobile-overflow.mjs
node scripts/verify-marketing-site.mjs
```

All green at the end of tonight. `verify-prod-current` is red by design.

**The machine-readable evidence:** `node scripts/gate-board.mjs` runs the
battery and writes `audit-out/gate-board.json` — last full run **60 of 61**,
the one red being `verify-prod-current`. Do not quote "all green" without
regenerating it.

**The print toolchain has an executable path too**, behind §16's warning that
the print block is fragile: `scripts/shoot-plan-pdf.mjs` renders the block to
`audit-out/pdf/` and screenshots its pages. Commit `588250b` fixed an Export
PDF that **crashed the editor** on some blocks (`(warm || []).filter is not a
function` — the normaliser is `asRows()`); regenerate and LOOK before
committing any print change.

**Prove a gate by breaking the fix.** Three versions of one gate once passed a
visibly broken page. Tonight the handoff audit was broken on purpose, and the
one lie that slipped through exposed a real hole.

### 13b · The performance numbers (2026-09-07)

Measured with `scripts/perf-probe.mjs` from the real seats at 1400px, production
(`audit-out/perf/prod.json`) beside the branch on :4173
(`audit-out/perf/branch-after2.json`). Bytes are on the wire.

| Seat · route | Production cold | Production warm | Branch cold | Branch warm |
| --- | --- | --- | --- | --- |
| owner · /coach | 4,373 KB · 112 req | 6,788 KB · 8 long tasks, 1,274ms | 656 KB · 104 req | 347 KB · 0 long tasks |
| owner · /coach/athletes | 3,989 KB | 3,153 KB | 613 KB | 287 KB |
| owner · /coach/bhbc | 4,360 KB | 7,290 KB | 661 KB | 312 KB |
| physio · /coach/bhbc | 783 KB | 197 KB | 540 KB | 198 KB |
| athlete · /athlete | 654 KB | 250 KB | 548 KB | 257 KB |

What the 12MB on the owner's routes was: `autoAnalyzeVideos` imports
`MovementLab`, which fetches MediaPipe's 3MB WASM from jsdelivr and its ~9MB
pose model from Google storage. Both are cross-origin, so the precache never
held them and they came down again on every page view — and then a pose pass
ran per pending clip on the main thread. The physio and athlete seats were
never heavy; the warm-load numbers on the branch overstate the cost, because
`vite preview` sends no cache headers and Vercel does.

One YouTube embed, measured bare (`audit-out/probe-yt-embed-cost.mjs`):
18 requests, ~1.2MB, last byte at 4.4s. The poster facade: 1 request, 37KB,
0.26s. From Diego's seat, opening a warm-up step on production mounts the
player; on the branch it mounts the poster (`audit-out/perf/yt-portal-*.png`).

Not changed, and worth knowing: the athlete seat fetches the whole exercise
library (`expo-exercises`, 133KB on the wire) on every load, and the service
worker precaches all 69 chunks (4.4MB) for every seat on first visit.

### 13d · The Shot Analyzer, after his four asks (2026-09-07 evening)

`audit-out/shot-results-look.mjs` runs a real clip (`public/testclips/clip02.mp4`)
through the tool from the owner seat in Hebrew and English and screenshots the
results (`audit-out/shot-results-he.png`, `audit-out/shot-results-en.png`), counting Latin words on the
Hebrew screen and jump buttons against checkpoint rows. Slow — a full MediaPipe
pass — and the only honest way to look at that screen.

### 13e · Hebrew coverage across the coach app (2026-09-07, the three hours)

`audit-out/probe-coach-coverage.mjs` signs in as the owner with the app in
Hebrew, walks every coach route from `docs/SURFACES.md` (`ROUTES=`), and
counts Latin words per route plus the most frequent English words across all
of them. The run of 2026-09-07 evening is in
`audit-out/perf/coach-coverage-0907.log`; the routes closed from it are in the
commits after `bd3a939`.

The numbers at the end of the shift (Latin words per route, owner seat, app in
Hebrew; what remains is names, e-mails, months, exercise names and file formats):

| route | before | after |
| --- | --- | --- |
| /coach (dashboard) | 177 | 39 |
| /coach/tasks | 244 | 12 |
| /coach/billing | 143 | 18 |
| /coach/review-tools | 44 | 15 |
| /coach/exercise-cleanup | 209 | 68 |
| /coach/calendar | 43 | 10 |
| /coach/sessions | 43 | 29 |
| /coach/smart-import | 39 | 11 |
| /coach/intake | 17 | 8 |
| /coach/bugs | 10 | 0 |
| /coach/waitlist | 24 | 22 — e-mails and lead sources |
| /coach/review | 44 | 15 |
| /coach/workouts | 43 | 43 — the list is exercise names; the logger's CUE / Completed / Complete Workout / LOG INTO went through the translator anyway |
| /coach/chat-audit | 177 | 171 — chat transcripts, i.e. content; the six labels went through |

**The athlete portal, tab by tab (01:15, Diego, Hebrew, `audit-out/probe-athlete-tabs.mjs`):** program 222 (exercise names and cue text) · weight 7 (plan and day names) · meal log 1 · history 1 · PRs 1 · messages 1 — the 1 is his name. Before this pass history said "No workouts yet", PRs "NO RECORDS YET …", messages "NOTIFICATIONS · Off. Tap Enable …" and "● REC / SEND →" in English. INSIDE a started workout (`audit-out/probe-athlete-workout.mjs`): 7 → 5, the 5 being the day name and the exercise name; SETS / REPS in the hero and the logger header were the two.

**Drill-downs (`audit-out/probe-coach-drilldowns.mjs`, 01:22–02:08, owner, Hebrew):** program editor 609 → 312 · Edit Athlete form 127 → 98 (e-mails and the format/package enums) · athlete detail 195 → 127 · booking settings 18 → 5 · review list 16 · the athlete portal preview 36 (exercise names). A route sweep counts the page a route lands on; the coach spends the day one click deeper.

**The whole coach app, measured once more at 01:05 on 09-08 on the final build** (`audit-out/perf/coach-coverage-final3.log`, 22 routes, owner seat, `expo-lang=he`): exercise-classify 7,606 · exercise-matching 3,512 · exercises 735 (exercise names and the canonical taxonomy, English by rule) · bhbc 208 (the zone keeps its OWN switch; with it, Medical reads 45) · chat-audit 171 (transcripts) · athletes 80 (e-mails) · exercise-cleanup 68 · programs 63 (plan names) · workouts 43 · sessions-single 43 · dashboard 39 · sessions 29 · waitlist 22 · billing 18 · review 15 · smart-import 11 · tasks 10 · calendar 10 · intake 8 · review-tools 1 · challenges 0 · bugs 0.

Auto-task bodies are stored in English (their idempotency keys depend on the
text) and re-said in Hebrew at render by `src/autoTaskHe.js` — ten templates,
names kept inside bidi isolates; anything the coach typed passes through untouched.

### 13f · Two sweeps on the 09-08 build (00:05–00:45)

`scripts/audit-console.mjs http://127.0.0.1:4173` — 35 routes from the manifest, one console error on every route and it is the same one: `/_vercel/insights/script.js` 404, the Vercel Analytics loader that only exists on Vercel (`audit-out/perf/console-0908.log`). `scripts/light-dark-parity.mjs` on the ten routes touched tonight (dashboard, tasks, sessions, bugs, smart-import, waitlist, workouts, review, athletes, /athlete): moved=0 on all ten; the dashboard reads countDelta=-5 both times, which is live data (messages, tasks) arriving between the two theme samples, not geometry (`audit-out/perf/ldp-0908.log`).
`verify-mobile-overflow.mjs` at 390 in HEBREW from all three seats (owner, pt, athlete): 0 elements cut off, each (`audit-out/perf/mobile-he-0908.log`, 00:53–01:07).

**The whole battery once more on the FINAL build** (`ff98d59`, 02:13–02:47, `audit-out/perf/gates-final-0908.log`): 15 steps, 15 exit 0 — collapse, pt no-backend, pt 390, pt raw values (en + he), the Hebrew gate, athlete journey, athlete no-backend, owner raw values (en + he), owner 390 (en + he), offline everywhere (23/23), athlete offline, the marketing site. Then the host chain (`audit-out/chain-final-0322.sh`) re-shot every pair on that build.

### 13c · Offline, every seat (2026-09-07, the hour)

`scripts/verify-offline-everywhere.mjs` walks each route twice - online, then
with the backend unreachable - and fails on a crash, a blank page, or silent
loss. It takes `SEAT=owner|athlete|pt|public`; the close chain had only run
the owner. The hour's run covers all four; the verdicts are in
`audit-out/perf/hour-chain.log`. Two things changed for it: the coach roster
snapshots for staff seats (a non-empty roster only ever exists on a staff
device; an athlete's session never restores one), and the dashboard's four
KPIs read "—" when the tab is offline with nothing cached, instead of "0".

### Probe traps that have cost real time

1. Clicking tabs by **English label** measures one tab five times in Hebrew.
   Click by INDEX; identical word counts on every line is the tell.
2. `sessionStorage.clear()` alone leaves the previous seat signed in — the
   Supabase session is in `localStorage`.
3. The zone has **its own** language switch (`עב`); `expo-lang` does not move it.
4. CSS `text-transform` hides words from grep — the DOM said `Update`, the
   screen said `UPDATE`.
5. Measuring straight after a viewport resize catches an empty frame.
6. Lazy images never render in a full-page capture.
7. A `file://` screenshot of a PNG hangs the shooter for its whole timeout.
8. The service worker reloads after a rebuild: "detached Frame" means retry.
9. `execSync` + `curl -w "%{http_code}"` → cmd.exe eats the `%`; use `http.get`.
10. `execSync` + `git cat-file -e ${sha}^{commit}` → cmd.exe eats the `^`.
11. **`_vercel/insights` 404 noise** — the analytics beacon 404s locally and a
    console gate counted **56 phantom problems** across two hooks. Filter it.
12. An element at `left:-9999px` is **hidden, not clipped**. Flagging it
    teaches everyone to ignore the overflow gate.
13. `execSync` has a **1MB default buffer**; the full commit log on this
    branch is larger and it throws the whole log back as an error.
14. **`page.setViewport()` is silently ignored on this Chrome.** Every probe
    must resize through `scripts/lib/viewport.mjs` (`setWidth(pg, w, h)`), which
    drives it over CDP. A probe that calls setViewport directly measures the
    window it happened to inherit.

### Not proven

`audit-out/probe-demo-dates.mjs` carries a header saying it is **not
trustworthy** and must not be cited as passing. Hebrew *register* is not
machine-checkable; coverage is.

---

## 14 · The request ledger — nothing lost

Status: **done** · **partial** · **open** · **cancelled by him** · **pacing**.

### The earlier block

| # | His words | Status |
| --- | --- | --- |
| 1 | "keep working on the bhbc display for programs and make the pdf export way better designed" | done; the PDF was redesigned again later (#16 below) |
| 2 | "this doesnt look anything like a bhbc branded page" | done |
| 3 | "also make sure every single page on any of our platforms also work offline" | done — offline gates for athlete, pt, owner, public |
| 4 | "in 2 hours when you're done update the local chrome host for comparison before and after" | done — the recap on :4179 |
| 5 | "make sure i can collapse all the boxes in bhbc" | done + `verify-bhbc-collapse.mjs` |
| 6 | "there shouldnt be a cyan border after the last name and adjust the space to the end of the card/box after you remove it" | done |
| 7 | "make sure the update button is alligned beneath the name or status button. perfectly ocd aligned" | done — measured |
| 8 | "after 4 hours of work, verify that everything you did doesnt go against any of the rules and my taste of preferance" | done |
| 9 | "log in the prveious practices and today francis worked out" / "nate worked out today" | done — 176 entries, attendance from medical dates, no invented RPE |
| 10 | "weight room lift, 30 minutes for francis 1 hours for nate" · "#2 but follow the history of medicals" | done |
| 11 | "keep working…", "restart the timer. work 5 hours…", "you worked for 10 minutes" | pacing |

### This conversation

| # | His words | Status |
| --- | --- | --- |
| 12 | "today dj and nate worked out for 30 minutes each" | **done** — DJ Burns (#30, his choice) + Nathan Knight, 30 min each, read back |
| 13 | "show me on a local chrome host everything that's chnaged and undeployed… perfectly viewable and designed perfect for me to judge" | **done** — :4181 |
| 14 | "i want a display on bhbc of who worked out when (and what players need to workout soon)" | **done** — the Weight Room tab |
| 15 | "[the sheet] that's litterally the main use of this table… plan a way better system im so not satisfied with what we currently have" | **done** — the replan at :4180 |
| 16 | "the pdf design is still too spacious and ugly, not branded enough as well" | **done** |
| 17 | "the weight room design is still not good enough and it should be a full tab" | **done** |
| 18 | "replan the entire bhbc system" | **done** — §5–8 |
| 19 | "and apply with way better thought and outcome" | **done** |
| 20 | "also replan the entire dashboard of bhbc (main page - some stuff unnecessary, some stuff can be combined, some are lacking…)" | **done** — 7 cards → 3 |
| 21 | "keep working for 4 hours to complete those tasks" | pacing — worked |
| 22 | "FOR LATER: show me before and after for every thing" | **partial** — the 09-06 and 09-07 changes have live-vs-branch pairs and a measured table on :4181; the undo-recipe builder still covers nine entries, not every recap entry |
| 23 | "keep doing then redo the local host" | **done** — rebuilt around live-vs-branch |
| 24 | "3 more hours of local work" → "cancel the 3 hour work" | **cancelled by him** |
| 25 | "update the local chrome host to be current, and add another section: chances that touch the athlete ui/expeiernce/portal and also show before after" | **done** 09-07 — the Hebrew pairs, phone-width pairs and a per-file athlete table on :4181 |
| 26 | "make sure everything on the localhost is aligned right to left for easy choice of before and after" | **done** 09-07 — equal-height panes, mirrored scroll by fraction, `audit-out/verify-pair-scroll.mjs` 4/4 |
| 27 | "can you create a full deatiled handoff for a new conversation… or is it too complicated at this stage and stuff will get lost?" | **done** |
| 28 | "write a perfectly audited handoff, fully detailed, x10 more detailed than actually needed" | **done** |
| 29 | "re-run tests… to check ten different times that the handoff is perfect" | **done** — four wrong claims found and corrected |
| 30 | "cancel the 3 hour work and finish the handoff prompts. make sure all undone work and qeued tasks are handed off" | **done** |
| 31 | "i basically want to continue everything as it is right now but on a different chat and let it work for 3 hours but we need a perfect handoff" | **done** — §18 |
| 32 | "run 10 more different handoffs and combine them all… audit it 15 times… choose one word" | **done** — `docs/handoff/01…10`, 15 passes, the word is `pita` |
| 33 | "nothing should ever be lost. all the details should be handed off to perfection" | **done** — this ledger; an audit pass fails if any row lacks a status |
| 34 | "do you think the handoff is perfect? verify three times and tell me the truth" | **done** — three passes; **five gaps found**, including no deploy procedure at all |
| 35 | "obviously every check should cover the entire document of handoff.. bullshit answeer" | **done** — `verify-handoff-full.mjs`: every line categorised, 100% accounted for |
| 36 | "do you think the handoff is perfect? verify four times… all of the document each time" | **done** — coverage measured, then closed rather than reported |
| 37 | "run one more perfect audit to make it perfect" | **done** — the ten lens files had never been audited; four verifier bugs found |
| 38 | "i mean agents and a dynamic workflow. perfect handoff is the goal. use all the resources available" | **done** — two workflows, 26 agents |
| 39 | "bhbc handoff? this is an entire conversation handoff. im confused" | **done** — the title was wrong; retitled and scoped in the opening table |
| 40 | "re-run the agents if you were wrong and only did a handoff for bhbc" | **done** — five-agent hunt; **31 confirmed omissions**, all fixed here |
| 41 | "way too many words on the local host i want more before and after pictures and less words" (earlier block) | **open** — a standing constraint: the hosts favour paired images over prose; keep it that way when rebuilding them |

### 2026-09-07

| # | His words | Status |
| --- | --- | --- |
| 42 | "i left a full handoff for you" | **done** — resumed from this file, §15 worked in order |
| 43 | "make sure you finish every single task on the handoff. then search for leftovers, undone things, half-done things, and finish everything" | **done** for everything that was mine to decide; what is his to decide is in §15.6 |
| 44 | "the app and the desktop expo and bhbc and all platforms have been very laggy and slow lately, check and fix it everywhere" · "maybe its just my wifi that's slow rn but check it out" | **done** — measured on production and the branch (§13b); it was not only the wifi |
| 45 | "yuvi's videos take a while to load fix it" | **done** — the YouTube facade (§13b) |
| 46 | "just please make sure that you're not interferring with the google pay chrome chats its important and another claude it dealing with it" | **done** — private Chrome on 9223 for every probe (§1) |
| 47 | "do everything except deploying yet. keep going for another full hour on your own" | **done** — the login screen in Hebrew with an EN/עב switch, the dashboard's offline KPIs as dashes, the coach roster cached offline for staff seats, the library read checked (intended). Not started, because it is a build and not an hour: analysing clips server-side |
| 48 | "make sure you add: offline perfectly working for all athletes and coaches & pt's" | **done** — the offline walk now runs for every seat (owner, athlete, pt, public), results in §13c |
| 49 | "to the shot analyzer: can you also add a makes/shots counter" | **done** — the coach marks each detected shot MADE/MISSED; the counter is those marks and nothing inferred (the analyser has never seen the rim); a Make column in the session table; saved with the session |
| 50 | "[screenshot of the Shot Analyzer in Hebrew] i want everything in hebrew here" · "specificially in this tool" | **done** — the title, the frame overlay, "fps", and the checkpoint names in the session-consistency panel and the focus line, which had printed the engine's English |
| 51 | "also the buttons are still a mess (stance to land, i need an equal spreading of the buttons across the two rows)" | **done** — the phase chips fill two full rows (4 + 3), equal width within a row |
| 52 | "too many play buttons next to the checkpoints that lead to the same frame" | **done** — one ▸ per frame: the first row of each phase keeps it |
| 53 | "what does elbow offset and wrist vs eye measures with? what's the units? it doesnt say anything" | **done** — both are in torso lengths (shoulder to hip); the tiles carry the unit and say what they measure on hover, both languages |
| 54 | "from knee to wrist vs eye.. all the numbers should be on the same vertical heights" | **done** — the label block reserves two lines; measured 0px spread across both rows of four |
| 55 | "keep going" (after the hour) | **done** — the review-tools page behind the analyser in Hebrew; the marketing card says the coach marks makes |
| 56 | "work for another 3 full hours autonomously on your own. keep going. nonstop. you have lots to do." | **done** — the Hebrew coverage sweep across 22 coach routes — dashboard 177→39, tasks 244→12, billing 143→18, review 44→15 Latin words; the rest is names, plan names, the finance channels and stored auto-task bodies; what it found and what was closed is in §13e |
| 57 | "keep going" (×2, during the three hours) | **done** — the sweep continued route by route; see §13e for the numbers |
| 58 | "update the local host chrome for me to judge before deploying with the rules we had" | **done** — :4179 recap / :4180 replan / :4181 tonight rebuilt on the final build (16 pairs mirrored + the six small coach routes), and opened in the persistent debug Chrome (9222, `chrome-debug-budget`, launched beside his own Chrome, nothing killed) |
| 59 | "no way you stopped working after 12 minutes. you have a ful hour of work!" | **done** — worked through to ~23:50: the last labels on sessions / bugs / smart-import, the exercise-cleanup tool, the auto-task bodies, the host refresh, this handoff |
| 60 | "amazing work. reset the timer to 3 hours and keep going nonstop. you have so much left to do so please resume autonomously until the end of the timer" (23:50) | **pacing** — the block ran 23:50 → 00:22, then he reset it (row 63); what it did is in §4b and the ledger rows after this one |
| 61 | "after youre done, at 3 hours: work for another 40 minutes on updating the local chrome host for reviweing before deploying" | **queued** for ~02:50–03:30: pictures over words, before/after for everything, mirrored pairs, opened in the debug Chrome |
| 62 | "also update: today zach, amit menachem worked out for 30 minutes, and gershon and knight worked out for 60 minutes (weight room all)" | **done** — four minutes-only Lift rows on 2026-09-07, 4/4 verified in the database, restore point in §10; the script re-run says all four already exist |
| 63 | "sorry im just now going to sleep. so reset the timer again please. make sure you follow the times. good luck. push and keep working on all of our platforms then finish like how i asked with the last 30 minutes" (00:22) | **done** — worked 00:22 → 03:15 (§4c), the 15-step battery green on the final build at 02:47, then `audit-out/chain-final-0322.sh`: 29 mirrored pairs on :4181 (login, the club zone, the athlete portal on desktop and phone, meal log, review tools, tasks, billing, dashboard, sessions, bugs, smart-import, calendar, intake, waitlist, workouts, review, the demo portal, the program editor, the Edit Athlete form, the athlete detail page), :4180 and :4179 rebuilt, the three tabs reloaded in the debug Chrome at 03:15; every commit pushed |
| 64 | "today amit gershon lifted for 1 hour, and nathan for 1 hour" (09-11 13:30) | **done** — two minutes-only Lift rows on 2026-09-11, 2/2 verified, restore point in §10 |
| 65 | "show me the chrome local host after it's perfectly designed and upgraded" · "update the local chrome hosts aftet fully done" | **done** — the servers had died with the shell (all six ports dark, 9222 down); restarted on 127.0.0.1 (vite binds localhost only — every probe uses 127.0.0.1), the debug Chrome relaunched with the three tabs, every pair re-shot on the day's final build, the three pages rebuilt and reloaded |
| 66 | "[screenshot] i don't want any focus anywhere there and never asked for it" | **done** — no FOCUS line under exercises, no COACH'S FOCUS label in the logger, the sandbox mock's focus card gone; shot from the athlete seat |
| 67 | "make sure that all the buttons are the same size and rulings as in the english versions. everywhere. full audit/sweep/fix" | **done** — measured (414 differences), two causes fixed (font line box, label width), re-measured; §8 and §13g |
| 68 | "my athletes are complanining that they get the update notification every single time they log-in the app, lets add some rules to it" | **done** — six rules in `SwUpdateBanner.jsx` (§8) |
| 69 | "keep going thx" | **pacing** |
| 70 | "after 1 hour have passed: deploy everything except athlete portal changes. everything everywhere but the expo athlete portal ui/experience/no changes there" (17:05) | **done, 19:45** — branch `deploy-0911` (`ClientPortal.jsx` + `MealLogger.jsx` at production, the athlete tree outside the language provider, the login English) pushed to master = `cf884fb`; restore point `dc80f2d`; prod current + smoke clean + both seats photographed (§3). Gates on the deploy build: athlete journey clean, pt no-backend clean, marketing 54 route/width combinations clean, Hebrew 0, owner raw values 0; athlete-no-backend "FAIL" is expected — production's portal has no offline notice, and that is the portal that shipped |
| 71 | "whatever is left: show me what's left to review regarding undeployed athlete portal on a local chromehost" | **done** — `scripts/build-athlete-left.mjs` → `audit-out/athlete-left.html` on :4182 (eight production-vs-branch pairs from Amit's seat in Hebrew: program desktop + phone, history, PRs, bodyweight, messages, meal log, the demo; the review list; the ship command), a tab in the debug Chrome |
| 72 | "[compare screenshot] this can be way better designed …" · "play both, pause, sync, etc.. redesign it smarter, nicer, ocd" · "the title … should both be center above the matching video" | **done, deployed** — §8 row for `WorkoutReview.jsx` (compare); photographed in both languages via `audit-out/probe-compare-modal.mjs` (`audit-out/compare-en.png`, `compare-he.png`): titles centred over their video, one transport bar, no METRICS. On `bhbc-hebrew` the modal also speaks Hebrew and the Hebrew date inside the Latin-first label no longer scrambles (bidi-plaintext per " · " segment) |
| 73 | "vs demo should only be an option on exercises with no previous logged videos" | **done, deployed** — VS DEMO renders only when COMPARE has no candidates |
| 74 | "as your last task before deploying: full audit for the hebrew, everywhere. it's not even close to being a good hebrew that doesnt sound like ai wrote it" | **done, deployed** — 1,335 shipped strings exported (`audit-out/export-he-strings.mjs`), every batch through the native judge (report only broken/translated lines), 57 repairs written by hand (`audit-out/he-fix-0911.mjs`, asserted one match each) and judged again (one agreement fix); three real errors (הקווויז, שבעת תבניות, עליהן), the rest bare imperatives → his future-imperative and office Hebrew → spoken. Cue corpus untouched |

---

## 15 · The queue — how to resume, in order

**Check the tree before you trust this line.** It was written clean and does
not stay that way — generated artifacts (`audit-out/tonight.html`,
`audit-out/handoff-coverage.json`, `docs/handoff-audit.md`) are rewritten by
the tools this document tells you to run:

```bash
git status --short
```

Everything in the queue below is a start, not a rescue.

### Done, verified, committed

logged DJ Burns + Nate · the Weight Room tab (grid · due strip · available row ·
what the room did) · the replan at :4180 · the dashboard 7 → 3 · contact minutes
→ density → Q1–Q4, proven and restored · the printed block · the host at :4181 ·
this handoff and its audit · **09-07:** the athlete-facing section and the
mirrored pairs on :4181 · the performance fixes and their measurement (§13b) ·
the YouTube facade · the meal log, the notes banner, the warm-up heading and the
bodyweight line in Hebrew · every browser script on `CDP=`.

### 1 — The athlete-facing section  ·  DONE 2026-09-07

*"add another section: chances that touch the athlete ui/expeiernce/portal and
also show before after"*

The screenshots exist already:

```
audit-out/pairs/portal-he-live.png    audit-out/pairs/portal-he-branch.png
audit-out/pairs/pt-zone-he-live.png   audit-out/pairs/pt-zone-he-branch.png
audit-out/pairs/pairs-he.json
```

In `scripts/build-tonight.mjs`, the `LIVE` const and `LIVE_TITLES`
map are at lines 23–24 and already read `pairs.json` and render a section per
pair. Add a second block reading `pairs-he.json`, with the substance in §11.

Two things that will bite, both verified by opening the file:

- **The `name` fields COLLIDE.** `pairs.json` and `pairs-he.json` both contain
  `portal` and `pt-zone`, so rendering both with the existing
  `id="live-${p.name}"` produces duplicate DOM ids. Suffix the Hebrew ones
  (`live-he-${p.name}`).
- **The paths in the manifest use Windows backslashes** —
  `audit-out\pairs\portal-he-live.png`. The existing code converts them with
  `.split(String.fromCharCode(92)).join('/')` before passing them to `img()`.
  Skip that and every image silently fails to load.

Then rebuild and **look**:
`node scripts/shoot.mjs http://127.0.0.1:4181/ audit-out/tonight-look.png 1400 1000 full 6000`.

### 2 — Align the pairs  ·  DONE 2026-09-07

*"make sure everything on the localhost is aligned right to left for easy choice
of before and after"*

In the CSS inside `scripts/build-tonight.mjs`: equal fixed height on both
`.shot` panes with `overflow:auto`; an inline script mirroring `scrollTop`
between the two panes of each `.pair` (guard the feedback loop with a flag);
images stay `width:100%` so both render at the same scale.

### 3 — Refresh all three hosts at the END of the block, not before  ·  `audit-out/chain-close-0907.sh` does it

```bash
node scripts/shoot-prod-vs-branch.mjs
LANG_APP=he JOBS="athlete:/athlete:portal,pt:/coach/bhbc:pt-zone" node scripts/shoot-prod-vs-branch.mjs
node scripts/build-tonight.mjs
node scripts/build-bhbc-replan.mjs
node scripts/build-recap.mjs
node scripts/audit-handoff.mjs
```

The servers die with the shell that starts them. Check them with `http.get`,
never `curl` through `execSync`.

### 4 — Before/after for EVERY recap entry  ·  PARTIAL, 13 reproduced + 25 live-vs-branch (09-08 00:30)

**Where it stands (09-08):** `scripts/build-before-after.mjs` holds 13 reproducible pairs — the nine from before plus `tasks-autobody`, `dashboard-dashes`, `roster-offline`, `bw-bidi` — and the recap at :4179 shows all 13. The :4181 host shows 25 live-vs-branch pairs, which ARE before/after for everything production does not have. The builder now sets the app language AFTER sign-in, snoozes the install prompt in storage, reads Hebrew nav words for the coach-seat check, and has a `clickText` step. What is still unpaired is the historical club-zone work (four bhbc pairs exist) — one pair per recap commit was never the useful unit; one pair per visible change is.

**Correction — historic "befores" DO exist.** `scripts/build-before-after.mjs`
reproduces them by checking out the OLD code and re-rendering, and nine
finished pairs are already in `audit-out/beforeafter/`. The job is to extend
that across `docs/SURFACES.md`, not to invent a method.

**One measured dead end, so nobody re-pays for it:** that builder cannot shoot
the athlete portal in Hebrew — both frames come back as the boot splash. The
live-vs-branch pairs at :4181 are the working path for that surface.

### 4b — What the 09-07 evening left for the next shift (in order)

1. ~~**The exercise-cleanup tool in Hebrew**~~ — DONE 2026-09-07 late: 209 → 68
   Latin words, the 68 are exercise names (`src/ExerciseCleanupView.jsx` has a
   translator; the reasons go through `tt(r.reason)`).
2. **The exercise taxonomy on the Exercises / Matching / Classify pages** — 739,
   3,516 and 7,610 Latin words, almost all exercise names (English by rule) and
   the canonical taxonomy words (Barbell, Standing, Push…). Translating the
   taxonomy is a decision, not a fix: the taxonomy is canonical and English.
3. ~~**Auto-task bodies**~~ — DONE 2026-09-07 late, the other way round: the
   stored English stays (idempotency keys depend on it); `src/autoTaskHe.js`
   re-says the ten templates in Hebrew at render (`displayBodyOf` in both
   `TasksV8View.jsx` and `taskFormat.js`). Tasks page 244 → 12 Latin words.
4. **The coach demo and the /try sandbox have no translator at all** — `src/CoachDemo.jsx` (259 Latin words with the app in Hebrew) and `src/TrySandbox.jsx` (a 1:1 mock of the portal) carry zero `tt()` calls; the Hebrew marketing site links to `expo-app.co.il/try`, so a Hebrew prospect gets an English sandbox. Two ways, his call: point the Hebrew CTA at `/demo/athlete?lang=he` (the real portal, Hebrew now — one link change in `expo-il/src/App.jsx`), or translate the mock and the coach tour (a session).
5. **The athlete detail page's coach-log and evaluation cards** still say COACH LOG, ACTIONS, ACTIVITY, EVALUATION, RECENT, CURRENT, LAST, DATE, NEW in Hebrew (127 Latin words remain on the page after the 02:00 pass) — their labels live in sub-components, not in `TraineeDetail.jsx`; `DETAIL=1 node audit-out/probe-coach-drilldowns.mjs` measures it. Half an hour.
6. **Roster positions in the club zone** (Guard, Forward-Center, Point Guard)
   are roster data; a coach reads them in English. Not touched.
7. The Latin words that remain on the coach routes after this shift are names,
   e-mails, plan names, months and the finance sheet's channel names.

### 5 — Parked on purpose

| Item | Why |
| --- | --- |
| S&C minutes per day (`8 Min + 6 Min`) | needs a per-day field; the plan text already has a home |
| Basketball Plan A/B on the day | session plans carry it already |
| The month grid on the dashboard | would re-create the duplication this replan removed — **ask him** |

### 5b — Before you stop, every shift

1. Run the gate set in §13 for whatever you touched — after a rebuild.
2. Update §3 (state) and §15 (queue) in this file, and re-run both verifiers
   until they are green.
3. Write a memory file for the session and add ONE line to `MEMORY.md`
   (keeping it under ~24KB — see rule 16).
4. Commit everything; leave no uncommitted work in `src/`, `scripts/`, `docs/`,
   then `git push origin bhbc-hebrew` so the night survives the disk.
5. Report: what changed, what was measured, what is still open, what needs him.

### 6 — Open questions only he can answer

1. **Deploy or not.** The whole branch — see the count in §3.
2. **Contact minutes** — will he type one number per practice?
3. **His Q4 label** — the legend says "Moderate Volume & High Intensity" while
   Q2 says "High Volume"; the app puts Q4 in the high/high cell.
4. **ACWR / readiness off the dashboard** until an RPE exists — agreed?
5. **The dashboard grid** — 14 days or a month?
6. Older and still open: caching the coach roster offline; dashboard offline
   zeros → dashes; what `›` means in Block #16's sets/reps; whether the login
   screen should speak Hebrew. (`/demo/coach` showing 6 nav items against the
   real 9 is a recorded decision in `src/CoachDemo.jsx`: the demo omits tabs it
   has no content for, "a dead click is worse drift than a missing one".)
7. New on 09-07: the auto-analyse warmer still analyses every new clip on the
   coach's own device; a server-side pass would take it off his machine
   entirely, and that is a build, not a fix. (The athlete seat reading the
   whole exercise library is by design: the portal builds each plan row from
   the library by id - `store_exercises_read` grants it to every seat.)
8. The YouTube facade needs one tap on iOS Safari before the player plays
   (an iframe cannot inherit the tap); Chrome and Android play on the tap.
9. Decided by him on 09-07 ("do everything"): the login screen speaks Hebrew
   (with a switch, since nothing is known about a visitor before sign-in); the
   coach roster is cached offline on staff devices; offline zeros are dashes.

---

## 15b · The three local pages — how to START them

**Start them — they die with the shell that launched them, and nothing else** 
**in this document gives the command. `serve-page.mjs` defaults to the REPLAN** 
**page on 4180, so the naive invocation serves the wrong page on the wrong port:**

```bash
node scripts/serve-recap.mjs 4179
node scripts/serve-page.mjs audit-out/bhbc-replan.html 4180
node scripts/serve-page.mjs audit-out/tonight.html 4181
```

| Port | Page | Built by | Served by |
| --- | --- | --- | --- |
| 4179 | `audit-out/recap.html` | `build-recap.mjs` | `serve-recap.mjs` |
| 4180 | `audit-out/bhbc-replan.html` | `build-bhbc-replan.mjs` | `serve-page.mjs` |
| 4181 | `audit-out/tonight.html` | `build-tonight.mjs` | `serve-page.mjs` |

Audit pass 4 pings all three and goes red if any is down — so a fresh shell
that has not started them gets three red checks that mean nothing.

## 16 · Risk

### Never

Push to `master` without asking · write to trainee-visible tables in a trial ·
invent an RPE, load, payment or session · bulk-touch the exercise library · use
"cure/diagnose/fix" in anything read by a person · `git add -A` · reconstruct
financial history into `bit_payment_requests`.

### Fragile

| Thing | Why | Do |
| --- | --- | --- |
| `src/BhbcView.jsx` | ~5,000 lines; three components share a closing `</Card>);}` shape | scope edits to a function's line range, never whole-file replace |
| `src/dates.js` | reaches App, ClientPortal, BhbcView, Billing, Booking, Challenges, Notes, Plans, CoachDemo | the Hebrew change was deliberately word-only |
| `src/readinessAutoreg.js` | SHARED with the athlete portal | translate at the render, never here |
| `themes.css` print block | one rule can add a page to every export | regenerate the PDF and look |
| The three local servers | die with their shell | restart before quoting a URL |
| The debug Chrome | leftover pages once corrupted a timing-sensitive analysis | `curl -s --noproxy '*' http://127.0.0.1:9222/json/list \| grep -c '"type": "page"'` |

### Looks broken, is not

`verify-prod-current` red until deploy · "nothing written" ×3 in *what the room
did* (no plans yet) · `3×>` in the printed block (real stored data in Block #16)
· the grid looking sparse in early September · `verify-hebrew` "0 files" meaning
zero files **with violations**.
· **one console 404 on EVERY route of the built app at :4173** — it is `/_vercel/insights/script.js`, the Vercel Analytics loader that only exists on Vercel; `audit-out/probe-404.mjs <url>` names the URL when the sweep does not. The 09-08 00:05 sweep (`audit-out/perf/console-0908.log`, 35 routes) had no other error.

### The largest open risk

The undeployed commit count is in §3 — stated once so it cannot drift, and the
audit re-derives it. The longer it grows the bigger the single deploy and
the harder a regression is to attribute. His call — but say it out loud each
time, do not carry it quietly.

---

## 17 · Where everything lives

```
src/BhbcView.jsx            the club zone (tabs, boards, modals)
src/bhbcHe.js               its Hebrew, composed not translated
src/dates.js                fmtPrettyDate / fmtCompactDate / fmtNumericDate
src/readinessAutoreg.js     readiness headlines — SHARED; not touched tonight
src/themes.css              @media print rules for the block export
src/PlansView.jsx           the printable block markup
docs/SURFACES.md            every route/tab; audits enumerate from it
docs/handoff/01..10         the ten source handoffs this file merges
docs/handoff-audit.md       the last green audit run
audit-out/bhbc-sheet/       his five month tabs as true CSV exports
audit-out/bhbc-state/       restore points for every store write
audit-out/pairs/            live-vs-branch screenshots + manifests
scripts/verify-*.mjs        118 gates
```

### Known gaps and honest caveats

- The demo probe is not trustworthy and says so in its own header.
- The weight-room grid stretches its columns; it tightens as the month fills.
- "Continious" is misspelled 10 times in his library data. **Not edited** —
  library data is never touched without his explicit OK.
- `verify-prod-current` is red by design.
- **Deferred translation.** `src/MealLogger.jsx` still shows English error
  strings, day labels and totals, and one WARM-UP heading could not be traced
  to its source. Both are known and neither is a regression.
- **An open design question, raised by commit `a333fe1` and never answered:**
  an EXPO athlete's live floor session is visible inside the BHBC club zone,
  because the two zones share one live channel. It is not a leak of medical or
  personal data, but a club coach can see that one of Ohad's private clients is
  training right now. **Ask him** whether that is intended.

---

## 18 · The audit, and the prompt

### Seventeen passes, and a second verifier

Two tools, not one:

- `node scripts/audit-handoff.mjs` — seventeen passes over the claims that matter.
- `node scripts/verify-handoff-full.mjs` — walks **every line** of this file and of
  the ten lens files, and puts each into verified / attributed / structure /
  judgement. 631 lines of substance here, 100% accounted for, 0 failures;
  the five judgement lines are printed by line number. Output:
  `audit-out/handoff-coverage.json`. It exists because measuring coverage at
  27% and REPORTING it was not good enough: *"obviously every check should
  cover the entire document of handoff.. bullshit answeer"*.

The first writes `docs/handoff-audit.md` and exits
non-zero on any mismatch.

| # | Pass |
| --- | --- |
| 1 | git — branch, the sha this file states is a real commit on it, drift in commits/files |
| 2 | every `src/` `scripts/` `docs/` `audit-out/` path named here exists |
| 3 | every script named documents itself |
| 4 | the three hosts answer 200 |
| 5 | the database matches — roster, fixtures, `contactMin` restored, lift totals — **and this file states those numbers** |
| 6 | memory files exist, tonight is indexed first, `bbq` survives |
| 7 | the gate set (run separately; results in §13) |
| 8 | every value quoted from his sheet is in the CSV; five months are five distinct files; note-lane counts match |
| 9 | the claimed behaviour is really in the source |
| 10 | nothing from this branch is on master; the open questions are still recorded |
| 11 | the ten lens handoffs exist and none contradicts this file on a key number |
| 12 | every request in §14 carries a status |
| 13 | the resume word is registered in this file, in memory, and in `MEMORY.md` |
| 14 | no weasel words — "probably", "should be", "I think", "almost always" |
| 15 | every fenced command block names a script that exists |
| 16 | the document may not contradict ITSELF — a count stated twice must agree |
| 17 | the dangerous procedures are present — deploy, rollback, verification, Chrome, the snapshot rule |

### What the audit caught in the first draft

"428 files" (scoped, not whole-tree) · "note lanes almost always empty" (45
filled cells; January is the most annotated) · "February dropped the PT lane"
(January did, earlier) · "`readinessAutoreg.js` untouched" (true of tonight
only). And a bad **source**: the month CSVs had come through gviz by sheet name,
which is lossy on a merged-cell sheet — the quotes had been read from a
different export. Fixed: true exports by pinned gid, and the downloader shouts
if two months come back byte-identical.

### It was broken on purpose

```bash
# plants one lie per pass, restores everything after
node scripts/break-test-audit.mjs
```

Ten lies, one per pass, planted and re-run. **10 of 10 caught**, and the
restored document still passes — a gate that only ever passes has proven
nothing.

| Planted lie | Caught by |
| --- | --- |
| "999 files changed" | pass 1 |
| a reference to a script that does not exist | pass 2 |
| "roster 11" | pass 5 |
| "August: 6 filled note cells" | pass 8 |
| "readinessAutoreg rewritten tonight" | pass 9 |
| a deleted lens handoff | pass 11 |
| a request row with no status | pass 12 |
| the resume word removed from MEMORY.md | pass 13 |
| "The grid probably works on a phone" | pass 14 |
| a command block naming a ghost script | pass 15 |

Two of those passes only bite because an earlier break test found them blind:

- **Pass 5** read the database, printed the truth, and never checked that this
  document agreed. "roster 11" sailed through.
- **Pass 9** passed as long as ONE sentence said the right thing, so a
  contradicting sentence elsewhere went unnoticed. Every line that mentions
  the shared file must now agree.
Three false claims planted: "511 files" (caught, pass 1), "August: 4 filled note
cells" (caught, pass 8), "roster 11" (**not** caught — the pass read the
database and printed the truth without checking that this document agreed). That
hole is closed; the same lie now fails.

### The prompt for the new chat

Say **`pita`** — or paste this:

```
Read docs/HANDOFF-2026-09-06.md end to end, then MEMORY.md and
project_bhbc_replan_2026_09_06.md, then git log --oneline -20. That is the
state; do not re-derive it.

Then work nonstop for 3 hours on the queue in section 15 of that handoff, in
order: the athlete-facing section on the local host, then aligning the pairs so
before and after line up, then the rest of the queue. Do not deploy anything.
Refresh the three local hosts at the END of the three hours, not before.

Rules that matter most: never test on the live site; snapshot before any write
to the store and restore after; never invent training data; verify from the real
seat and LOOK at the screenshot before saying something is done.
```
