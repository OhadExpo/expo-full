# Working list — 2026-09-01

Live task list. Updated as each item lands, with the measurement that closed it.
Ohad's rule for this session: everything synced, working online, 0 bugs,
designed properly — and every audit run for **mobile** as well as desktop.

## Done, measured

| # | Task | Evidence |
|---|---|---|
| 1 | Rehab entries "deleted" | Never saved — `addProgress` wrote local state only. Dialog is `sticky` (no Escape/✕), entries edit + delete. |
| 2 | Availability clicks doing nothing | Next value came from the rendered row. 3 clicks in one tick applied **1 → 3**. |
| 3 | Cards: dead air after last row | Portal history 9/17 → **9/9** (126→118px); shared `Card` −4px; `EmptyState` `<p>` margin. |
| 4 | Text cut off | **101 → 1** at 1600px. Exercise titles, `oneCell()`, chips, couples' emails (197px hidden), demo too. |
| 5 | HEY vs אוהד height | Hebrew was 17px vs 15px *on top of* the face's `size-adjust`. Both 15px, ink **10.0 vs 10.0**. Source-swept: only occurrence. |
| 6 | No FOCUS on real portal | Read only the sparse weekly store; **81% of rows (3,457/4,264)** already carry the cue. Falls back to it. |
| 7 | EXPO icon on BHBC | 44×48 source forced into 16×16 (squashed). Aspect kept, 19px, 55%, brightens on hover. |
| 8 | Ink centring | **0 rows** app-wide at 1600px, now including icons and controls. |
| 9 | MED column widths | `+ MED` 55px / `MED ✎` 58px → both **62px**. Availability already 108. |

## Open

| # | Task | State |
|---|---|---|
| A | **Every sweep at mobile widths** (390/414) — ink centring, card trailing, text clipping, button heights | next |
| B | Left blue/orange border must not run alongside the card TITLE — only the white body, starting under the title | not started |
| C | "Open in EXPO" / "View program" — same, or owner-only; programs viewable read-only on the BHBC site | not started |
| D | Full history table: no borders or row dividers | not started |
| E | The pop-up card design overall | not started |
| F | Shot analyzer 4–5× better | precise re-run wired (deterministic capture: **754 frames vs 639**, 24.5 vs 20.7 fps, same wall time); needs a forced-starve verification |
| G | Text-overflow sweep **re-run after all design work** (design changes create new overflow) | queued last |
| H | Marketing + demo parity for everything above | rolling |

## Standing gates (all green unless noted)

`verify-row-button-heights` · `light-dark-parity` · `verify-ink-centring` ·
`verify-no-text-clipping` · `verify-card-trailing` · `verify-shot-determinism` ·
`verify-shooter-lock` · `verify-clip-usable` · mobile overflow
