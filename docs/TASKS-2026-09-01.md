# Working plan — 2026-09-01, 02:21 → 12:21

Ohad: everything synced, working online, 0 bugs, designed properly; every audit
also run for **mobile**; all data bulletproof backed-up; full security pass.

**Ordering principle:** protect the data first, then prove it is safe, then make
it correct, then make it beautiful. A design fix can wait a night. A lost
athlete record or an open door cannot. Nothing is "done" until a measurement
says so, taken from the real seat.

---

## Phase A — bulletproof backup (FIRST)

Nothing else matters if the data can be lost. His words: "all the information
that we have, and all the info, details, logs, videos, text, anything... is
bulletproof saved and backed-up."

- [ ] A1 Inventory every store of record: `store` keys, `plans`, `client_workouts`, `bw_logs`, `coach_messages`, medical, BHBC keys, Storage buckets (form videos, voice notes)
- [ ] A2 Measure the true size of each — a backup that cannot finish is not a backup
- [ ] A3 Full export to a dated, verified snapshot; **restore-verify** by reading it back and diffing row counts + a content hash
- [ ] A4 A repeatable script, committed, so this is a habit and not a one-off
- [ ] A5 Storage objects (videos) covered explicitly — they are the part a DB dump misses

**Done when:** a snapshot exists, its row counts and hashes match live, and the
script can be re-run by one command.

## Phase B — security pass, all platforms

- [ ] B1 RLS on every table: no anon read of athlete data; coaches scoped to their own
- [ ] B2 Storage bucket policies — form videos and voice notes must not be world-readable
- [ ] B3 Secrets: no service-role key in any client bundle; check `dist/` as shipped
- [ ] B4 Auth surface: password reset, session handling, the BHBC coach tier
- [ ] B5 Supabase advisors clean

**Done when:** each item has a query or a fetch proving it, not an assumption.

## Phase C — finish the athlete pop-up card

- [x] C1 History dividers — were `0.25px`, cannot paint at 1×. 30 hairlines app-wide now 1px
- [x] C2 History outer border, inset rows, ✎/✕ in one fixed column
- [x] C3 "OPEN IN EXPO" vs "VIEW PROGRAM" — already correct, verified from BOTH
      seats: owner sees both, a coach sees only `View program` (openInExpo 0).
      And `View program` IS the read-only BHBC-side view he asked for.
- [x] C4 Pop-up: dropped the footer's duplicate `Medical report` (same onInjury
      as UPDATE six rows above, and the 4th button wrapped its own label);
      three buttons at 209px. Metric tiles say WHY they are empty instead of
      three 22px dashes. History cap 118 -> 134 so it stops slicing row 4.

## Phase D — every sweep at MOBILE widths

390 and 414, every route **and** every BHBC tab. Measured, not assumed:

| Gate | 390 | 414 | What it found |
|---|---|---|---|
| ink centring | 0 | 0 | clean |
| row button heights | 0 | — | clean |
| page overflow | 0 | 0 | docW within viewport at 360/390/414 |
| text clipping | **58 → 1** | — | the 1 is a deliberate collapsed cell |
| card trailing | 148 → real ones fixed | 167 → same | see the note below |

The header was the real find: at 390 `nav.hdr-scroll` measured **12px wide**,
so the coach could not navigate at all. Now 316/346/370 at 360/390/414.

Card-trailing needs reading with judgement at phone width. Its 6px threshold
is calibrated for desktop cards that declare padding; at 390 most cards
declare none and let rows carry the spacing, so the 10-15px tail is rhythm,
not dead air (checked against a screenshot before touching anything). The
REAL ones were the 224-256px cluster from `alignItems:'stretch'` rails and
the 114px demo board column - both fixed. What is left above 20px is the
chat-audit transcript panel, which reserves its height on purpose.

## Phase E — marketing + demo parity

`/demo/*`, `/try`, `/coaches/*`, `/he/*`, `expo-il/`. The demo keeps its own
copies — the email clipping fix already had to be made twice.

## Phase F — shot analyzer

Measurement only. Ruled out: frame-by-frame capture (found **9 of 17** shots on
his own clip, 3× slower). Next: coarse-pass window detection; whether the fine
pass can hold source cadence without starving.

## Phase G — full regression + close out

All gates, both themes, desktop + mobile. Prod verified by chunk hash. Memory
current. Written summary.

---

## Closed today, each with the measurement that closed it

| Task | Evidence |
|---|---|
| Rehab entries "deleted" | Never saved — local state only. Dialog `sticky`; entries edit + delete. |
| Availability clicks | Next value read off the rendered row. 3-in-one-tick: **1 → 3**. |
| Card dead air | Portal history **9/17 → 9/9** (126→118px); shared `Card` −4px; `EmptyState` `<p>`. |
| Text cut off | **101 → 1** at 1600px. Titles, `oneCell()`, chips, couples' emails; demo too. |
| HEY vs אוהד | 17px vs 15px atop the face's `size-adjust`. Both 15px; ink **10.0 / 10.0**. |
| No FOCUS on real portal | **81% of rows (3,457/4,264)** carry the cue; falls back to it. |
| EXPO icon | 44×48 squashed into 16×16. Aspect kept, 19px, 55%, hover. |
| Ink centring | **0 rows** at 1600 **and** 390, including icons and controls. |
| MED column widths | 55/58px → both **62**. |
| Card rail on the title | Moved to the body: 0px under the strip, at the card edge. |
| History dividers | 0.25px → 1px, 30 places. Outer border; action column at one x. |
| "Precise" capture | **Withdrawn** — 9 of 17 shots on his clip, 3× slower. |
