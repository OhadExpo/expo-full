# Working plan — 2026-09-01, 02:21 → 12:21

Ohad: everything synced, working online, 0 bugs, designed properly — and every
audit run for **mobile** as well as desktop. Written down, followed in order,
updated as each item lands with the measurement that closed it.

## Phase 1 — finish the athlete pop-up card (now → ~03:30)

| | Task | State |
|---|---|---|
| 1.1 | History rows had no dividers — they were `0.25px`, which cannot paint on a 1× display | **done**, 30 hairlines app-wide now render |
| 1.2 | History list needs an outer border; ✎/✕ actions need one aligned column | in progress |
| 1.3 | "OPEN IN EXPO" vs "VIEW PROGRAM" — unify, or make one owner-only | next |
| 1.4 | Whole pop-up: spacing, the empty ACWR/7-day/28-day strip, footer button rhythm | next |

## Phase 2 — programs readable on the BHBC side (~03:30 → 05:00)

Pull the program from EXPO, render it read-only in the BHBC zone. No editing, no
writes. Coach-visible only where the tier allows.

## Phase 3 — every sweep at MOBILE widths (~05:00 → 06:30)

390 and 414, every route in SURFACES.md **and** every BHBC tab:
ink centring · card trailing · text clipping · button heights · page overflow.
Desktop is already green on all five; mobile is the gap.

## Phase 4 — marketing + demo parity (~06:30 → 08:00)

Everything shipped today re-checked on `/demo/*`, `/try`, `/coaches/*`, `/he/*`
and `expo-il/`. The demo carries its own copies of several cards — the email
clipping fix already had to be made twice.

## Phase 5 — shot analyzer (~08:00 → 10:00)

Grounded in measurement only. Already ruled out: frame-by-frame capture (on his
own 17-shot clip it found **9 of 17** and took 3× as long). Next candidates, each
to be measured before it ships: shot-window detection on the coarse pass, and
whether the fine pass can run at source cadence without starving.

## Phase 6 — full regression (~10:00 → 11:30)

All gates, both themes, desktop + mobile. Fix whatever they surface.

## Phase 7 — close out (~11:30 → 12:21)

Verify prod live by chunk hash, memory/handoff current, written summary.

---

## Closed earlier today, with evidence

| Task | Evidence |
|---|---|
| Rehab entries "deleted" | Never saved — local state only. Dialog `sticky`, entries edit + delete. |
| Availability clicks | Next value came from the rendered row. 3-in-one-tick: **1 → 3**. |
| Card dead air | Portal history **9/17 → 9/9** (126→118px); shared `Card` −4px; `EmptyState` `<p>`. |
| Text cut off | **101 → 1** at 1600px. Titles, `oneCell()`, chips, couples' emails, demo too. |
| HEY vs אוהד | 17px vs 15px on top of the face's `size-adjust`. Both 15px, ink **10.0 / 10.0**. |
| No FOCUS on real portal | **81% of rows (3,457/4,264)** carry the cue; falls back to it. |
| EXPO icon | 44×48 squashed into 16×16. Aspect kept, 19px, 55%, hover. |
| Ink centring | **0 rows** at 1600 **and** 390, including icons and controls. |
| MED column widths | 55/58px → both **62**. |
| Card rail on the title | Moved to the body: 0px gap under the strip, at the card edge. |
