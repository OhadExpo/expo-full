# EXPO handoff — 2026-06-09 (for the next Claude session / audit)

You're picking up EXPO (Ohad's fitness-coaching platform: Vite + React, Supabase, Vercel auto-deploy on push to `master`). Read `CLAUDE.md` and the memory index `MEMORY.md` first — especially `project_session_2026_06_09.md`.

## Where we're at (shipped + verified live this session; master tip `fd326a4`)

- Program editor compare view now breaks out of the global `<main>` `max-width:1200px` to `min(96vw,2400px)` when compare is active, so both panes fit the full 9-column day grid (no overflow / clipped-column whitespace). `src/PlansView.jsx` ~815.
- Drag-to-reorder in the editor: the whole day grid is the drop zone; the insertion line tracks the cursor by each row's vertical centre (`data-exrow` on handles); downward drops no longer land one slot too low. `src/PlansView.jsx` day-map ~949.
- `/coach/review`: the "N pending with form videos" summary is clickable → scrolls to and force-expands the first pending athlete's section; each expanded athlete section has a "View athlete page →" button; the REVIEW QUEUE strip height now matches the athlete section strips. `CollapsibleSection` gained optional `domId` + `openSignal` props. `src/WorkoutReview.jsx`, `src/ui.jsx`, `src/App.jsx`.
- DELETE already has a confirm modal (Cancel + type-"delete" Delete) in both list and detail — no change needed.

## What to audit (fresh eyes — full multi-lens sweep)

- Enumerate every surface from `docs/SURFACES.md` (audit from the manifest, not memory).
- Prioritize `/coach/review` and the program editor (`PlansView.jsx`) — this session's churn. Check the compare grid alignment, drag UX, and the new review buttons across light/dark, RTL Hebrew names, couples (sub-ids), and narrow widths.
- Cross-cut: pixel alignment (`getBoundingClientRect`, not eye); uniform card strips (open sweep ticket — canonical `padY = 14`); RLS/security (never change brand or RLS unilaterally); light/dark parity.
- Use `reference_full_audit_prompt.md` and `reference_ultrathink_audit_pattern.md` as the recipe.
- Verify-before-done is the hard rule: deploy, confirm the new bundle hash is live, inspect actual rendered DOM/pixels, then claim it works.

## Environment

A Chrome instance is attached on `--remote-debugging-port=9222` (Ohad's profile), logged in as trainer → COACH. Drive it via the chrome-devtools MCP.
