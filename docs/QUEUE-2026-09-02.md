# Open queue — 2026-09-02 (live tasking)

**SHIPPED 2026-09-02 21:35** — he lifted the order himself (*"figure it out
perfectly, triple check in different options then deploy"*). 38 commits,
`082e87b..9de96cc`. **Restore point `082e87b`.** Verified on prod: new bundle
live, athlete portal renders, PT board + editor intact, and a real BHBC coach
opens an athlete programme.

## Done and verified

| Ask | Evidence |
|---|---|
| BHBC shows each athlete's EXPO block in a pop-up, read-only | `ProgramModal`; 0 inputs in the card |
| Pop-up looks like Sessions > Group | white cards, hairline borders, bold name, prescription on its own line, numbered |
| Only the current week | taken from the highest week logged for that athlete |
| Block chips → a picker like EXPO Programs | `BLOCK #4 · 2 PREVIOUS ▾`, verified on Daeshon Francis |
| No cues in the pop-up | "why does it show me the notes and focus i never asked for it" |
| **Coaches and PTs can see the programs** | RLS applied; coach seat 0 → 8 plans |
| EXPO icon: no black ground, boxed like the theme toggle, full colour | `expo-icon-lg.png` was mode RGB with no alpha at all |
| Dashboard flipped to light mode | BHBC called `setTheme('light')` on mount, rewriting his saved pref everywhere |
| Sweep for anything like it | `verify-theme-stability.mjs`, 2 routes → 0 |
| Medical names/injuries aligned in columns | grid `10px / 96px / 1fr` |
| Load-board text overflow | mobile grid used `1fr` = `minmax(auto,1fr)`; 0 overlapping rows at 470px |
| "Open in EXPO" off the last button row | moved onto the block line it refers to |
| Sessions: numbered exercises | number always; tick replaces it when done |
| Sessions: `0/5 DONE` aligned with `5×4` | ink centres were already 0.10px apart — they needed a shared BASELINE |
| `ON THE FLOOR` title placement | strip centre 76.1, buttons exact, title ink 75.6 — nudged |
| NEXT GAME row | 25px above / 0 below → 12 / 12, symmetric at any wrap count |
| `Log session` looked unstyled | `--c-ghostBd` is declared once on `:root`, so it cannot recompute for a subtree theme — my regression |
| Analysis graphs overflow | lift names in THE ARC were ellipsised; 1 → 0 at 390 |
| **Export a block to PDF** | editor More menu → Export PDF; 4 tables, 27 rows, real vector text |
| Symmetry sweep | 3 ragged button pairs equalised; label-centring 0 off-centre at both widths |
| FOCUS published library cues to athletes | root-caused to commit `60428eb` and reverted |

## Applied to production (approved)

**`bhbc staff read bhbc plans`** — BHBC coaches and PTs may SELECT the plans of
BHBC-team athletes. Approved by him with the condition that it must not affect
athletes or EXPO, and it cannot: PERMISSIVE (OR'd, so additive only), SELECT
only, gated on the existing `is_bhbc_coach()` allowlist, scoped to 8 of 231
plans. Verified from the coach, athlete and PT seats. Full write-up and rollback
in `docs/rls-bhbc-staff-read-plans.sql`.

## Still open

- **Revenue always up to date** — pull twice a day from the Google Sheet
  (`18TdfofxAOd1d_EkOjbhYOBjWflqlfkAzY8sI52xJnOc`, gid 1803423381). Needs a
  scheduled job plus sheet credentials; both are outward-facing, so they need
  his go-ahead separately from the never-deploy order.
- **Program analysis** — he wants it kept going. The overflow is fixed; the
  next question is what he actually wants the graphs to SHOW.
- **`2026/27` in BHBC prose** — the top-menu chip is gone; what remains is
  data-driven sentences ("THE 2026/27 SEASON NOT STARTED"). Ask before stripping.
- **Shot analyzer corpus** — c01 2 swaps, c09 3, on edited footage whose
  same-scale cuts the detector cannot see. Known limit; never fires on his own
  single-take clips.
