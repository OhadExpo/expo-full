# Open queue — 2026-09-02 (live tasking)

Everything below was asked during the 09-02 session. **Nothing is deployed** —
his order: *"never ever deploy it."* All work is local on `bhbc-hebrew`.

## Done and verified

| # | Ask | Evidence |
|---|---|---|
| 1 | BHBC shows each athlete's EXPO block in a pop-up, read-only | `ProgramModal`; 0 input/textarea/select in the card; opens from the athlete modal |
| 2 | Pop-up looks like Sessions > Group | white cards, hairline borders, bold name, prescription on its own line, cue clamped, numbered |
| 3 | Only the current week, not W1–W4 | week taken from the highest week logged for that athlete; shown as a label |
| 4 | EXPO icon: black ground off, in the same bordered box as the theme toggle | `expo-icon-lg.png` was mode RGB (no alpha at all); `-t` variant made from luminance |
| 5 | Icon full colour, better aligned | it was dimmed in three places at once; square box, centred |
| 6 | Dashboard flipped to light mode | BHBC called `setTheme('light')` on mount, which writes localStorage AND `user_metadata.theme_pref`. Zone light is scoped to the zone now |
| 7 | Sweep for anything like it, all platforms | `scripts/verify-theme-stability.mjs` — diverges local from stored, walks every route. 2 → 0 |
| 8 | Medical names/injuries in aligned columns | grid `10px / 96px / 1fr`; injury takes its own line on a phone |
| 9 | Text overflow on the load board | mobile grid used `1fr` = `minmax(auto,1fr)`; 0 overlapping rows at 470px |
| 10 | "Open in EXPO" off the last button row | moved onto the block line it refers to |
| 11 | Sessions: number the exercises | number always, tick replaces it when done |
| 12 | Sessions: "0/5 DONE" centred with "sets × reps" | `lineHeight: 1` so the boxes hug the caps — optical, not box, centring |
| 13 | Sessions: "ON THE FLOOR" title alignment | same cap-height fix |

## Blocked on Ohad

- **Coaches and PTs cannot see the programs.** Measured from a real coach seat
  (`benshemer4@gmail.com`): `plans` and `plan_index` both return **0 rows with
  no error** — RLS hides them. The policy is written and reviewed at
  `docs/rls-bhbc-staff-read-plans.sql`, scoped to SELECT on BHBC-team athletes
  only. **Not applied** — it is a production database change.

## Still open

- **Export a block to PDF** (EXPO coach, inside a program). "Works perfectly and
  produces a perfect template of the block."
- **EXPO program analysis** — keep building it; the graphs need better display,
  some text/numbers overflow.
- **Revenue always up to date** — pull twice a day from the Google Sheet
  (`18TdfofxAOd1d_EkOjbhYOBjWflqlfkAzY8sI52xJnOc`, gid 1803423381). Needs a
  scheduled job and sheet credentials; both are outward-facing, so they need his
  go-ahead as well as the never-deploy order lifting.
- **NEXT GAME row** — he flagged it as too close to the bottom / not centred /
  box too big. Measured: the row itself is balanced (8px above, 9px below,
  36px tall), so the space he is seeing is ABOVE it, between the card's strip
  header and the first row. Not yet fixed.
- **Symmetry sweep** — "full sweep and audit for anywhere that you can make it
  more symmetric like here". The medical rows are done; the rest is not.
