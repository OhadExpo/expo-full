# 5-hour plan — 00:52 → 05:52, 3 Sep 2026

Reset by Ohad at 00:52. Restore point for everything tonight: `9de96cc`
(last deployed SHA). 13 commits sit on top of it, undeployed.

**Deploy status: NOT DEPLOYED. Ask before any push to master.**

## Rules I am holding to this run
- Never break the athlete experience on EXPO or the PTs on BHBC.
- Every "done" grounded in observed output, and every gate PROVEN by breaking
  the fix first.
- One task at a time, finish + verify + commit before the next.
- Mobile is measured with real emulation at 390/470, never `setViewport`.

## Blocks

### A · 00:52–01:20 — close out the sweep already running
- [x] gates green after tonight's layout changes (mobile-fit 0, roster 0,
      view-switch 0, strip-bleed 0, clipping 0)
- [ ] update docs/QUEUE and report status to Ohad

### B · 01:20–02:35 — SHOT ANALYSIS "300% better"
His concrete past defects are closed (hand/shot params re-score and say so).
So this block is about the analysis OUTPUT, measured on his own clips:
- [ ] audit the analyzer screen for overflow/clipping at 1500/900/470/390
      (the same audit that found two off-screen tables in block analysis)
- [ ] the release/dip/set checkpoint strip — he reported it overflowing; verify
- [ ] make the verdict readable on a phone (headers, tables, chips)
- [ ] run scripts/verify-shot-determinism + shot-stability to confirm no
      regression in what the numbers say

### C · 02:35–03:50 — BLOCK ANALYSIS "1500% better"
Mobile is fixed; the remaining gap is what it SHOWS.
- [ ] THE SKIPS tile renders an em-dash and nothing else — either give it a
      real read or drop the tile
- [ ] four sections are collapsed by default, so the screen's value is hidden;
      decide what opens by default on the evidence, not by taste
- [ ] the title strip exceeds its box by 0.8px at 390 — fix while in there
- [ ] sparkline legibility at 390 (they are 88x24 for a full block history)

### D · 03:50–04:40 — BHBC full-project sweep, every tab at 4 widths
- [ ] overview / roster / schedule / medical / sessions / games at
      1500 / 900 / 470 / 390, both languages, light and dark
- [ ] fix what it finds; add gates for anything that can regress

### E · 04:40–05:30 — the asks I could not close alone
- [ ] LAN access (needs an elevated shell — write the exact command for him)
- [ ] revenue twice-daily sync (blocked on ONE action from him: share the sheet
      with the service account, or publish that tab as CSV) — have the job
      written and tested against a fixture so it runs the moment he does it
- [ ] Hebrew coverage pass on everything touched tonight

### F · 05:30–05:52 — handoff
- [ ] memory + handoff file written from disk evidence
- [ ] state exactly what is undeployed and what deploying would change
