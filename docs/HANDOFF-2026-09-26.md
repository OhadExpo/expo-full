# Handoff — 2026-09-26, 15:00

This file is public (the repo is). It names commits, files, gates and numbers —
never a client, a health note or a credential. The private state lives in
`expo-private-backups/queue/` (`QUEUE-LIVE.md`, `DEPLOY-0923-BLOCKED.md`,
`DEMO-RUN-SHEET.md`).

## 0. Where things stand

| what | where | state |
|---|---|---|
| production (`expo-app.co.il`) | `origin/master` = `a69b360` | moved twice on 26.9: portal RLS hotfix `d528b7b` (14:13), athlete safety nets `a69b360` (14:45). Both smoked from the athlete seat against production: clean. |
| the work branch | `bhbc-hebrew` | **41 commits ahead of master, unpushed**, and no longer a plain fast-forward (it carries the two hotfixes as cherry-picks `c1b5450`, `eb77a3a`). To ship it: `git fetch`, `git rebase origin/master` on the branch (the duplicated cherry-picks drop), re-cut the athlete-portal hold with `bash scripts/cut-deploy-tree.sh`, then push. **Never merge origin/master into the branch.** |
| rollback target | `a69b360` | `git fetch` first, always. |
| subagent quota | — | exhausted until Sep 29 01:00 (Asia/Jerusalem). Everything below was done without agents; the pending BHBC code review is by hand. |

## 1. What shipped to production today

- **`d528b7b`** — an athlete saw *SAVE FAILED — EXPO-TRAINEES* over a completed
  workout. `useSupaStore.save()` had no same-reference short-circuit, so the
  portal's `prev => prev` session decrement upserted an empty roster to the
  staff-only store row from the athlete's seat; RLS refused the INSERT. The
  workout had saved (`client_workouts`). Two guards, two files, fifteen lines.
- **`a69b360`** — the athlete's safety nets: `src/seatWrite.js` (a write the
  seat cannot make never leaves the device and never becomes a banner), the
  10-second boot watchdog with a cache-clearing Reload, and three gates —
  `verify-portal-write-boundary` (in `npm run build`), `npm run verify:portal-rls`,
  `npm run smoke:portal`. Read `docs/ATHLETE-SAFETY-NETS.md`.

**The post-deploy ritual from now on:** `npm run verify:portal-rls` →
`BASE=https://expo-app.co.il npm run smoke:portal` → only then "done".

## 2. What is on the branch and not yet live (41 commits)

The 24.9 night and day, in order of weight:

1. **One control height (36px) across the product** — demo, real app, sales
   site, BHBC: `--btn-h` was 32/40 while the demo used 36. `681a15a`,
   `07095dd`, `1a8cbd6`, `257423f`. Gate: `scripts/verify-control-heights.mjs`
   (one bucket, every bordered element; rows ≥ 36 with glyph ink centred;
   `--site il`, `--site coach` signed in). Read `docs/DEMO-GATES.md`.
2. **BHBC rebuilt to the owner's model** — `178752a`, `72bfe34`, `6eeb379`: the
   team S&C session logged with its practice (attendance from availability),
   the personal lift on any date, no practice plans, `kind:'sc'|'lift'` on
   every row with `rowKind()` for legacy rows, Weight Room → Lifts, popups and
   header fit on tablet/phone. Its "two hours" deadline was missed on the API
   session limit, not on the work.
3. **The demo, looked at** (74 screenshots, both languages, 1440 + 390): header
   note, attention strip, programs/review cards, per-athlete weeks, MARK PAID
   border, exercises column, rail buttons. The header rebuilt to the real app's
   shape (`0336503`). Shot analyzer off the sales site and the demo (`70a41de`).
4. **Gates that now measure what they claim** — `a3b9ddc`, `9c52477`: every
   browser gate emulates its width (`setViewport` was ignored above the phone
   breakpoint, so every earlier "1440" figure was 1024 — all four app gates
   re-run at the real 1440 on 24.9 evening: heights 120/120 · 1,642 controls ·
   8 athlete-held findings; overflow 192/192 · 34,086 nodes · 0; bidi 30/30 ·
   1,153 tokens · 0; pages 144/144 · 604 scroll positions · 2 COVERED, see §4);
   the sales site's own language key; ROWCENTRE measured on text nodes; a JSX
   duplicate-prop gate in the build.

## 3. How to resume

`git fetch && git log -1 origin/master` — if it is not `a69b360`, someone
pushed; read before anything. Then `expo-private-backups/queue/QUEUE-LIVE.md`
top to bottom (the numbered tasks #178–#210 are the 24.9–26.9 story with
evidence), then the memory index. The one-word triggers `pita` / `bbq` still
apply.

Preview: `npm run build && npx vite preview --host 127.0.0.1 --port 5199`; sales
site `cd expo-il && npx vite preview --host 127.0.0.1 --port 5174`. Debug Chrome
on `:9222` with the persistent profile. After any killed script:
`node audit-out/_close-leaked-contexts.mjs` (closes only throwaway contexts,
never his tabs). Run heavy browser jobs one at a time — two at once ran the
machine out of memory on 24.9.

## 4. Open, in priority order

1. **His calls, never guess:** hero numbers on the landing and sales site
   (measured 25 / 248 / 1,332 vs published 20+ / 90+ / 500+); contrast palette
   (`verify-contrast` left failing on purpose, 8 colour pairs); VAT wording on
   the coach tiers; MOST POPULAR on an empty tier; the athlete-portal deploy
   hold (its ✓ tag / KG field are the only height findings left); BHBC
   "full makeover everywhere" — model and fit were done, a visual redesign was
   not; commit `1481f1c`'s message carries funnel counts into the public repo
   (cheap to scrub only before the branch push).
2. **Pages gate COVERED ×2 at the real 1440** — the demo athletes rail's
   "+ Add Athlete" button under the waitlist band; a z-index on the sticky rail
   (`6eeb379`) did not clear it. Probe with `elementFromPoint` before guessing
   again.
3. **Demo tables at 390** — the dashboard roster shows all 9 columns (693px in
   a 357px wrapper; the real app drops seven); the exercises table's single
   name column is 151px against a void (a `width:100%` rule is not taking).
4. **BHBC hand review** (no agents until Sep 29): the S&C save path (attendance
   key with an empty `start`, re-save duplicating `sc` rows, `av >= 4` players
   skipped), `CourtAttendanceTab.isCourtRow` counting `sc` rows as court time,
   i18n orphans in `src/bhbcHe.js`.
5. **Smoke depth** — `smoke:portal` starts a day and exits; completing one would
   write fixture data every run. The completion path is covered by the seat
   fence + the RLS clause, not end to end. A runtime break-test of the fence
   itself is still to be written.
6. **The branch push** (§0) — rebase, re-cut the hold, `verify:portal-rls`,
   `smoke:portal` on the preview, push, `smoke:portal` on production.

## 5. Lessons written down today (memory has them; read them there)

- A zero must say what it measured: the 1440 that was 1024; the sales site's
  language key; the row-centre check that read cell boxes as ink; the login
  screen detected by a word the sessions floor also uses.
- A break test that passes is a finding about the gate.
- A same-reference update must be a no-op at the store, not just at React.
- Never `setViewport` on the attached Chrome; `setWidth()` and it throws if
  the width did not take.
- Never leave emulation on one of his tabs; throwaway contexts only.
